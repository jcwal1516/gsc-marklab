mod multiscale_embedding_artifact_graph {
    #[cfg(feature = "parquet")]
    mod derived_region;
    mod direct_graph;
    mod matrix_receipts;
    mod physical_receipts;
    mod resources;
    mod support;
}
