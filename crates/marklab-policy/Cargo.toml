[package]
name = "marklab-policy"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Machine-readable execution and maturity policies for Marklab"

[lib]
bench = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
