[package]
name = "marklab-numerics"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Stable bounded numerical primitives for Marklab"

[lib]
bench = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
