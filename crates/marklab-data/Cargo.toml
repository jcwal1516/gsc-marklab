[package]
name = "marklab-data"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Typed cohort hierarchy and data contracts for Marklab"

[lib]
bench = false

[dependencies]
marklab-core = { version = "0.1.0", path = "../marklab-core" }
thiserror = "2"
