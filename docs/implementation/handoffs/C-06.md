# Task Handoff — C-06 Bounded Measurement, Embedding, and Declared-Workflow Slices

Status: eight bounded vertical slices complete; FND-04/C-06/WS-50 remain active
Base SHA for the declared probability–cell-embedding covariance milestone: `ec9ca190b45c04d8e7e844201dee725f783c608d`
Final SHA: the local milestone commit containing this handoff; resolve with `git log -1`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only researchers: `c06_contract_map`, `c06_caller_map`; sole declared-probability-covariance milestone reviewer: `c06_caller_map`

## Scope delivered

- Added `MeasurementStatus::{Measured, ImportedPrediction, MorphologyPrediction, DerivedSummary}` in `marklab-data`, independent of `EmbeddingStatus` extraction validity.
- Bound exact C-05 provenance: direct patch extraction is morphology prediction; deterministic region and both slide variants are derived summaries.
- Added one immediate public caller, `patch_overlap_embedding_dispersion`, over the existing patch table, patch support, overlap graph, and provenance.
- Validates declared status plus table/provenance/support/expected-patch/footprint/overlap identities before numeric work.
- Enforces checked `edge_count * dimension` work, excludes non-present endpoints without exposing filler vectors, retains present all-zero vectors, accumulates fixed-order `f64`, and returns typed `InsufficientPairs` rather than NaN.
- Reports total/eligible/excluded counts, dimension, status, numeric availability, and table/support/overlap/provenance identities.

The approved wording is “descriptive mean squared Euclidean embedding distance across declared overlapping sampled patches.” It is not a variogram, Moran's I, Geary's C, spatial autocorrelation, tissue-window result, inferential endpoint, biological similarity, real-source result, or EMB-01 closure.

## Declared scalar-pattern workflow delivered

- Added one borrowed `DeclaredScalarPatternInput` for the exact binary and optional dense probability arrays already consumed by `AnalysisEngine`; `Pattern`, PatternLoader, config 0.2, result 0.3, CLI, dependencies, and physical formats are unchanged.
- Requires strictly ordered typed CellIds, one owning slide, an exact installed physical `[X,Y]` micrometre frame, present rows, positive caller row/text limits, exact measurement status, and exact C-03 mark/threshold provenance records.
- Supports independent binary origin or exact `>`/`>=` probability thresholding with matching status, source ID, row values, evidence dependencies, and distinct semantic roles.
- Direct execution delegates to `analyze_pattern_run`. `DeclaredMarkUse` truthfully reports probability only for structure-factor spectra/value permutations and binary for every other current endpoint.
- `DeclaredScalarIdentity` exposes row count, ordered-CellId digest, owning slide, frame, and the full declaration digest without copying numeric or CellId arrays into results.
- A separate `DeclaredMarkedAnalysisNode` binds the compatibility Pattern, declared identity, config, and semantic artifact IDs; it revalidates the target project before catalog mutation and verifies the bound store before cache lookup.
- Misses and hits reattach the node-bound identity/routing while the codec retains only exact result-0.3 bytes.

## Patch-to-derived-region aggregation dispersion delivered

- Added one concrete `patch_region_embedding_dispersion` caller over the exact source patch table, patch-region link, deterministic region candidate, copied verified graph, and direct plus derived provenance values.
- Exact source/link/candidate/graph/owning-slide/dimension/expected-set/support/provenance binding failures precede the checked `relation_count * dimension` work cap.
- Eligible relations use producer-declared fractions and materialized `f32` region means, accumulate sequential `f64` squared distances in canonical relation/component order, exclude non-present sources, retain present zero vectors, and return typed `InsufficientContributors` without NaN.
- The fixed-size result exposes total/eligible/excluded counts, dimension, exact source-table/link/candidate/direct+derived-provenance identities, and truthful `MorphologyPrediction` to `DerivedSummary` lineage.
- The claim is descriptive aggregation dispersion only. Component order is frozen; repeat execution and per-axis sign flips are bitwise stable, while arbitrary axis-order bitwise invariance is explicitly not claimed.

## Runtime-only declared marked pre/post delivered

- Added one concrete `compare_declared_marked_prepost` caller over two existing `DeclaredMarkedAnalysisResult` scheduler outputs; it adds no scheduler node, cache codec, durable result, physical format, receipt, validator, or generalized comparison surface.
- Each public runtime wrapper must have result row count equal to declared row count, result label equal to its binary declaration, and an exact scalar-identity digest recomputed from its attached mark use before cross-output comparison.
- The semantic gate requires exact binary mark ID/label/status, endpoint routing, optional probability presence/ID/status, origin class, threshold source ID/comparator, and threshold `f32` bits. Category errors expose no identifiers or values.
- Binary, probability, and threshold-evidence artifact IDs may differ; both mark-use summaries, both compact CellId/slide/frame/declaration identities, and borrowed exact timepoints are retained. Rows/slides/frames/timepoints may differ without implying correspondence or a biological unit.
- Compatible outputs delegate unchanged to `compare_marked_prepost`; all metadata flags, typed unavailable sections, diagnostics, interpretation, and result-0.3 bytes remain exact.
- The pre-existing runtime result has public fields and result 0.3 carries no private declared-input proof. Arbitrary same-row/same-label numeric-result substitution therefore remains caller-asserted and is not described as durable receipt authority.

## Declared binary prevalence change delivered

- Added one concrete `compare_declared_marked_prevalence` caller over two existing declared scheduler outputs without changing the public C-06-S4 wrapper or result format 0.3.
- Reused the exact semantic gate and strengthened both declared comparison callers with `n_marked <= n_cells`, finite canonical `p_hat`, and exact count-derived prevalence bits, including positive zero for empty rows.
- Always reports binary mark counts/prevalence even when probability values were routed to structure-factor spectra. Different rows/slides/frames/evidence/timepoints remain allowed and visible without implying correspondence.
- When both sides contain rows, reports exact signed `post - pre` prevalence from one `f64` subtraction. Otherwise returns typed `InsufficientCells`, retains both finite prevalences, and performs no delta arithmetic.
- The result is fixed-size and borrows both declared contexts. It adds no allocation, node, codec, result/config field, schema, format, receipt, validator framework, randomization, p-value, inference design, geometry owner, dependency, or external path.
- The claim is descriptive marked-row proportion change only—not paired-cell, patient/specimen prevalence, treatment effect, calibration, inference, equivalence, noninferiority, causality, or biology.

## Slide aggregation-path discrepancy delivered

- Added one concrete `slide_embedding_aggregation_path_discrepancy` caller over both completed C-05 slide candidates, their graphs/provenance, and the existing region-table receipt that proves one ancestral patch lineage.
- Every candidate/path/common-lineage binding and singleton row identity precedes the exact `D` work cap; status/vector traversal follows it so missingness cannot evade admission.
- Present singleton paths report fixed-order mean squared component discrepancy; any non-present path returns typed `InsufficientComparablePaths`. The result is fixed-size and adds no candidate, graph, receipt, physical profile, validator, serializer, general statistics abstraction, or path preference.

## Declared binary cell-embedding centroid discrepancy delivered

- Added one concrete `declared_binary_cell_embedding_centroid_discrepancy` caller over the existing declared scalar input and verified C-04 cell table/artifact.
- Exact table/artifact QC, row count, and every ordered CellId bind before conservative `present_rows * D + D` work and exact `2 * D * sizeof(f64)` storage caps. A local domain-separated digest identifies the exact CellId-bound binary assignments; optional probabilities remain visible but never select groups.
- Available inputs report the fixed-order `f64` mean squared component difference between present marked/unmarked centroids. Either group without a present vector returns typed `InsufficientGroups` after the same caps and before accumulator allocation/arithmetic; every embedding status remains exactly counted.
- The fixed result retains both declarations, compact scalar identity, grouping digest, embedding/row-link/model-provenance artifact IDs, QC, dimension, and logical table identity. It adds no mark kind, embedding/scalar infrastructure, physical format, receipt, graph, validator framework, serializer, node, result/config/CLI field, general statistic, or source path.
- The claim is descriptive centroid difference only—not spatial association, classification/separability, quality, independence, patient/specimen effect, inference, real-source evidence, WS-50/EMB-01 closure, or biology.

## Declared probability–cell-embedding cross-covariance energy delivered

- Added one concrete `declared_probability_cell_embedding_cross_covariance_energy` caller over the existing dense declared probability input and verified C-04 cell table/artifact.
- Required probability values, exact table/artifact QC, row count, and every ordered CellId bind before conservative `2 * present_rows * D + 2 * D` work and exact `2 * D * sizeof(f64)` storage caps. A local domain-separated digest identifies all ordered raw probability bits; binary rows remain visible context but never select or weight the endpoint.
- Available inputs report fixed-order two-pass `f64` population mean squared component cross-covariance energy. Fewer than two present rows and constant present probabilities have distinct typed unavailable states after the same caps and before accumulator allocation/component arithmetic; exact QC and the present probability mean remain visible.
- The fixed result retains both declarations, compact scalar identity, probability-value digest, embedding/row-link/model-provenance artifact IDs, QC, dimension, present count/mean, and logical table identity. It adds no mark kind, scalar/embedding owner, physical format, receipt, graph, validator framework, serializer, node/codec, result/config/CLI field, general statistic, or source path.
- The claim is descriptive unstandardized covariance energy only—not correlation, explained variance, spatial association, classification/separability, calibration, quality, independence, patient/specimen effect, inference, real-source evidence, WS-50/EMB-01 closure, or biology.

## Verification

| Gate | Result |
|---|---|
| Behavior-first status target | expected missing-symbol red; final 1/1 pass |
| Behavior-first computation target | expected missing-symbol red; final 6/6 pass |
| Focused no-default integration | 7/7 pass |
| Data package | 1/1 plus docs pass |
| Embeddings all-feature package | 56 unit + 6 domain + 1 hostile integration plus docs pass |
| Focused and package all-feature Clippy with `-D warnings` | pass |
| Strict root/data/embeddings public docs | pass |
| Single independent review | no concrete findings; no edits or second review |
| Sole all-feature workspace Nextest | 853/853 pass in 64.524 s; 23 skipped; one slow |
| Formatting and diff whitespace | pass |

### Declared scalar milestone

| Gate | Result |
|---|---|
| Behavior-first input/workflow targets | expected missing-surface reds; final default 9/9 + 8/8 pass |
| Focused no-default targets | 9/9 + 8/8 pass |
| Relevant API/config/project/result/workflow compatibility | 41/41 pass |
| Focused and workspace all-feature Clippy with `-D warnings` | pass |
| No-default workspace check and strict `-D warnings` root docs | pass |
| Single independent review | one runtime row/frame identity finding reproduced and fixed; no second review |
| Sole all-feature workspace Nextest | 870/870 pass in 64.923 s; 23 skipped; one slow |
| Broader root `-D missing-docs` probe | non-green on pre-existing compatibility API documentation; all new public fields documented; not claimed as a gate |

### Region aggregation-dispersion milestone

| Gate | Result |
|---|---|
| Behavior-first focused target | expected missing-surface red; final 6/6 pass |
| Complete derived-region integration | 69/69 pass; affected 6/6 rerun after review correction |
| Embeddings package | 56 unit + 6 domain + 1 hostile integration plus docs pass |
| No-default, warning-denied Clippy, strict docs, formatting | pass |
| Single independent review | one invalid axis-permutation claim corrected to sign-flip-only truth; no other finding or second review |
| Final all-feature workspace Nextest | 876/876 pass in 69.194 s; 23 skipped; one slow |

### Declared marked pre/post milestone

| Gate | Result |
|---|---|
| Behavior-first focused target | expected missing-surface red; final 4/4 pass |
| Affected default targets and legacy pre/post units | 34/34 plus 13/13 pass |
| Focused no-default declared targets | 21/21 pass |
| Warning-denied focused/workspace Clippy, no-default workspace check, strict docs, and formatting | pass |
| Single independent review | public-wrapper binding and missing-timepoint visibility findings corrected; no second review |
| Final all-feature workspace Nextest | 880/880 pass in 64.416 s; 23 skipped; one slow |

### Declared binary prevalence milestone

| Gate | Result |
|---|---|
| Behavior-first prevalence filter | expected missing-surface red; final 4/4 pass |
| Complete focused target | 5/5 pass in default and no-default modes |
| Affected declared workflow/result targets and legacy pre/post units | 26/26 plus 13/13 pass |
| Warning-denied focused/workspace Clippy, no-default workspace check, strict docs, and formatting | pass |
| Single independent review | eager unavailable-path subtraction replaced by an explicit nonempty branch; no other finding or second review |
| Final all-feature workspace Nextest | 881/881 pass in 64.648 s; 23 skipped; one slow |

### Slide aggregation-path discrepancy milestone

| Gate | Result |
|---|---|
| Behavior-first discrepancy filter | expected missing-surface red; final 5/5 pass |
| Complete derived-slide and combined graph boundaries | 16/16 and 74/74 pass |
| Embeddings package | 56 unit + 6 domain + 1 hostile integration plus docs pass |
| No-default workspace check, strict docs, warning-denied focused/workspace Clippy, formatting | pass |
| Single independent review | row-ID binding moved before the exact-D cap; no other finding or second review |
| Final all-feature workspace Nextest | 886/886 pass in 64.649 s; 23 skipped; one slow |

### Declared binary cell-embedding centroid milestone

| Gate | Result |
|---|---|
| Behavior-first centroid filter | expected missing-surface red; final 4/4 pass |
| Complete affected CellViT artifact graph | 29/29 pass; affected 4/4 rerun after review correction |
| Declared scalar input and embeddings package | 9/9; 56 unit + 6 domain + 1 hostile integration plus docs pass |
| No-default workspace check, strict docs, warning-denied focused/workspace Clippy, formatting | pass |
| Single independent review | missing exact binary-assignment identity corrected with a local digest and swapped-assignment regression; no other finding or second review |
| Final all-feature workspace Nextest | 890/890 pass in 64.580 s; 23 skipped; one slow |

### Declared probability–cell-embedding covariance milestone

| Gate | Result |
|---|---|
| Behavior-first covariance filter | expected missing-surface red; final 5/5 pass |
| Complete affected CellViT artifact graph | 34/34 pass in 34.46 s |
| Declared scalar input and embeddings package | 9/9; 56 unit + 6 domain + 1 hostile integration plus docs pass |
| No-default workspace check, strict docs, warning-denied focused/workspace Clippy, formatting | pass; focused Clippy's duplicate test-module finding was corrected without an allowance |
| Single independent review | no concrete finding; no edits, tests, or second review |
| Final all-feature workspace Nextest | 895/895 pass in 64.693 s; 23 skipped; one slow |

## Explicit deferrals and risk

- Current `Pattern` still has no native canonical CellId/status/provenance fields; the new Rust boundary supplies them without mutating Pattern or inferring a file adapter.
- General categorical/ordinal/count/continuous/simplex/vector/posterior columns, arbitrary units/modalities, scalar missingness, and physical schemas remain unimplemented.
- Patch overlap is declared extraction-support adjacency, not an admitted tissue observation window or formal spatial-weights contract.
- Formal EMB-01 requires geometry/window/weights/null/oracle/calibration work. No real patch artifact is promoted.
- Region aggregation dispersion uses producer-declared link fractions and proves neither region geometry nor independent patch evidence. Arbitrary axis reordering can change last-bit rounding because component order is a frozen numeric input.
- Declared pre/post permits different rows, slides, frames, and timepoints but establishes no row correspondence, paired-cell effect, biological unit, or population inference. Its retained context is runtime-only, and the existing public result wrapper cannot authenticate arbitrary same-row/same-label numeric substitution.
- Declared prevalence uses supplied binary row counts only. It is not patient/specimen prevalence or a treatment effect, and a caller can still substitute count/prevalence fields consistently because no private producer proof exists.
- Slide path discrepancy proves one exact common patch lineage and compares two materialized aggregates only. It is not agreement, quality, information preservation, geometry, tissue-window support, spatial dependence, path preference, real-source evidence, or biology.
- Declared cell-centroid discrepancy uses supplied binary labels and present embedding rows from one exact input. It is not spatial dependence, separation/classification, independent validation, embedding quality, a patient/specimen estimate, inference, or evidence that morphology-predicted marks are independent of their embeddings.
- Declared probability covariance uses supplied dense probabilities and present embedding rows from one exact input. It is unstandardized and not correlation, explained variance, spatial dependence, calibration, prediction, independent validation, a patient/specimen estimate, inference, or evidence that morphology-derived probabilities are independent of their embeddings.
- A result-only random-labeling execution summary was rejected: result 0.3 lacks exact base-seed/config binding, strata, endpoint namespaces, and executed counts for every current route. Do not revive it as execution provenance without complete producer-supplied inputs and an immediate caller.
- Exact registry packaging remains blocked by DEC-0018, and Windows store durability remains pending under DEC-0021; neither was changed or rerun.

## Immediate-Caller audit

No unused infrastructure was added. The first slice's shared enum is consumed by provenance and computation. The second slice is consumed directly by the unchanged marked engine and existing local scheduler; its compact identity/routing summaries are observable on direct, miss, and hit outputs. The third slice directly consumes the completed C-05 finalizer candidate and verified graph; its only table addition is a private row lookup used in that call. The fourth slice directly consumes two scheduler results; its only identity addition is a crate-private recomputation check used by that function, and exact timepoints are borrowed. The fifth slice directly consumes the same outputs and reuses their private compatibility gate; its result is fixed-size and fully borrowed. The sixth slice directly consumes both completed slide candidates and the existing region receipt; it adds no lineage or physical infrastructure. The seventh slice directly consumes the existing declared input and verified cell artifact; its local grouping digest is returned by that computation. The eighth slice consumes the existing dense probability and verified cell values; its local raw-value digest is returned by that computation. Shared declared-scalar test support is declared once because both immediate child test modules use it. Test-only digest/routing getters were removed. No serializer, physical schema, receipt, validator framework, dependency, benchmark framework, or speculative extension point exists.

## Next action

Freeze one dedicated `DeclaredBinaryCellEmbeddingCentroidNode` whose exact `LocalScheduler::run_single_with_store` execution is the immediate caller of a private node-local version-one codec. Revalidate declared and embedding semantic inputs before every cache lookup; preserve S7 numerics and result identity; add no engine/CLI convenience wrapper, generalized codec/result framework, or result-0.3 change. FND-02 remains deferred until exact source/frame/topology/window prerequisites and a caller coexist.
