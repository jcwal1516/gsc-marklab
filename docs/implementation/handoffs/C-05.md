# Task Handoff — C-05 Patch, Region, and Slide Embeddings and Links

Status: complete as bounded synthetic data infrastructure; real-source promotion remains prohibited
Base SHA: `e3dadae179957d7d4704a00531ed07f29b919934`
Final SHA: the local self-referential milestone commit containing this handoff; resolve with `git log -1`
Implementation SHAs: `4e1d5b5` through `966acaa`, plus the final closure commit
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; closure contributors: `c05_fuzz_closure_map`; sole closure reviewer: `c05_scale_closure_map`

## Scope delivered

Parent requirement IDs: DATA-01, FND-05, EMB-PATCH, WS-24, C-05.

- Added distinct, bounded patch, region, and singleton-slide embedding tables with exact expected sets, status semantics, logical identities, and patch extraction context.
- Added once-per-patch footprints, deterministic overlap components, vector-free grouped cell-patch edges, and exhaustive producer-declared patch-region relations. Links never copy patch vector components.
- Added strict source identity/normalization, support, derivation, producer/assessment, and four provenance variants with exact canonical wires and managed graph validation.
- Added all eight exact C-05 Arrow IPC and Parquet families with raw-before-stock bounds, deterministic publication, borrowed/managed parity, and graph/candidate-bound runtime receipts.
- Closed direct patch authority, deterministic fixed-order weighted-region finalization, and both arithmetic-mean slide finalization paths under explicit work and memory limits.
- Extended the existing embedding fuzz target across context, footprints/overlap, three typed tables, both links, strict provenance, and all eight Arrow/Parquet preflight families.
- Added one private declarative differential across all three tables, arbitrary scan partitions, both formats, and borrowed/managed validation.
- Added one private checksum-pinned patch/shared-link Criterion workload and its DHAT caller. The 10k smoke, 100k closure scale, allocation, and RSS gates pass.

C-05 and EMB-PATCH complete the synthetic multiscale storage/link substrate. They do not establish a real source adapter, source-vector or coordinate correspondence, geometric region truth, tissue observation windows, embedding quality, technical-confounder analysis, prediction, or biological conclusions.

## Closure-owned files

- `Cargo.toml`, `benches/patch_embeddings.rs`, `benches/support/patch_embeddings.rs`: immediate Criterion caller and private workload.
- `tests/patch_embedding_heap.rs`: immediate DHAT caller.
- `tests/multiscale_embedding_differential.rs`, `tests/workflow_contract.rs`: declarative oracle and target/route contracts.
- `fuzz/fuzz_targets/embedding_inputs.rs`: seven bounded C-05 routes in the existing target.
- `docs/implementation/**`: DEC-0038, corrected exact test owners, closure ledgers/status/requirements/claims/map, and this handoff.

No production Rust file, public API, physical format, validator, receipt, dependency, lockfile, workflow, source adapter, result/config/CLI field, or corpus object changed in the closure milestone.

## Behavior-first and review evidence

| Boundary | Red/review finding | Final behavior |
|---|---|---|
| Target discovery | Criterion and C-05 fuzz contracts failed on absent target/routes | Existing workflow contract passes with one benchmark and existing-target routes |
| Deterministic receipts | Zero placeholders intentionally failed only after independent numeric and link oracles agreed | Both profiles pin table/link/numeric/link-checksum identities in DEC-0038 |
| Shared-vector work | Review found the four-bucket index checks more candidates than edge count | Exact maxima are 391,510 and 3,987,010; one edge per assignment remains |
| Checksum reproduction | Review found ID and field framing underspecified | Exact ID bytes, field order/types, domains, and 128-bit framing are frozen |
| Cross-format parity | Existing suites lacked one all-table declarative oracle | One matrix agrees across tables, partitions, Arrow/Parquet, and borrowed/managed paths |
| Allocation/RSS | Per-edge vector copying could hide behind logical edge counts | DHAT peak is below the copied smoke payload; full RSS is below the copied closure payload and host cap |

The closure used exactly one independent review. No additional review was requested because no new security, unsafe-code, numerical-correctness, concurrency, parser, or breaking-public-API risk emerged.

## Key commands and results

| Command/gate | Result |
|---|---|
| Focused tables/links/records/graphs/physical/differential/package tests | pass: 12; 7 + 9; 7 + 63 + 21 + 8; 11 + 12 + 1; embeddings 22 unit + 6 domain |
| `cargo +nightly fuzz check` | pass |
| `cargo +nightly fuzz run embedding_inputs ... -- -runs=20000 -max_len=4096` | pass; 20,000 runs, no crash, `cov 16432`, `ft 30248` |
| Focused patch Criterion smoke | pass; 29.961–30.261 ms, all four receipts pinned |
| Exact patch DHAT integration gate | pass; current 0, peak 180,729,962, cap 335,544,320 bytes |
| Frozen timed `--no-run` prebuild | pass; 5,221,138,432-byte compile/link RSS |
| Frozen prebuilt 100k/1m Criterion run | pass; no compilation, 283.76–284.82 ms, 1,311,342,592-byte RSS |
| Eight-row compile matrix; all-feature and CLI-only `-D warnings` Clippy | pass; only documented narrow warnings outside Clippy |
| Strict embeddings docs; workspace doctests; WSI integration | pass; WSI 10 passed/1 scheduled public oracle ignored |
| Audit; deny; Machete | pass; no vulnerability, configured warnings only, no unused dependency |
| Legacy DHAT; workspace quick benchmark; release synthetic smoke | pass; 3/3; nine targets; 12/12 scenarios and 120/120 replicates |
| `cargo +1.96.0 nextest run --locked --workspace --all-features` | pass; 846/846, 23 skipped, one slow, 64.632 s |
| Dirty package characterization | expected non-green DEC-0018 after six archives; ephemeral local-path verification passes but is non-equivalent |
| Formatting and diff whitespace | pass |

The frozen `multiscale_embedding_provenance` command initially failed because that historical target no longer exists. The task contract now names its four responsibility-split record/graph targets, all of which pass. The precommit exact package command stops at Cargo's dirty-manifest guard; the exact clean command is run immediately after this self-referential commit and reported before C-06 work.

## Performance and numerical evidence

- Smoke: 10,000 × 1,024 vectors, 100,000 assignments/edges, ten cells sharing each patch vector.
- Closure: 100,000 × 1,024 vectors, 1,000,000 assignments/edges.
- Both use fixed row/column order, scalar `f64` sum and sum-of-squares, exact link traversal, and immutable checksums.
- Full runtime RSS is 48.8% of the 2.5-GiB cap; DHAT returns operation-owned current bytes to zero.
- These are storage/link workload checks, not an optimization, scientific estimator, model-quality, or biological-performance claim.

## Tests not run and remaining risks

- No authorized patch/region/slide candidate was promoted or component-decoded; source identities, frames, stride/overlap/receptive field, tissue support, model-run provenance, source-vector correspondence, and licenses remain incomplete.
- Opaque source coordinates were not proven to equal persisted anchors, and producer region fractions were not proven against FND-02 geometry.
- The scheduled public Aperio/OpenSlide oracle remained ignored; no network corpus mutation or executable model deserialization occurred.
- Exact registry packaging remains release-blocked by DEC-0018. Ephemeral local patches do not make it green.
- Windows durable-store target evidence remains pending under DEC-0021.

## Decisions and assumptions

- DEC-0032 through DEC-0037 govern typed tables, support/links, physical proof, candidate-only derived authority, and both slide paths.
- DEC-0038 governs closure shape, arithmetic, checksums, DHAT ownership, RSS thresholds, and commands.
- The source corpus is reference-only; C-05's real-source claim ceiling is unchanged.
- Existing C-01–C-04 identities, coordinates, artifact/store behavior, and cell-embedding goldens remain authoritative.

## Next exact action

Freeze and implement only the minimum C-06 measurement/status contract that has an immediate production caller, then use C-05/C-06 in an observable end-to-end scientific computation or user workflow before adding further infrastructure.

## Scope audit

- Existing user changes, result 0.3, config 0.2, CLI, scientific methods, remote assets, and current C-04 import behavior changed: no.
- Production infrastructure generalized without an immediate caller: no; closure additions are private to Criterion/DHAT/tests or routes in an existing fuzz target.
- Security, deterministic order, resource ceilings, raw-before-stock parsing, compatibility, or assertions weakened: no.
- New worktree, dependency, global install, registry tuple, publish, push, deploy, history rewrite, remote mutation, or secret persistence: no.
