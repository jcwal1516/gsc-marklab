# Task Handoff — B-01

Status: complete
Base SHA: `5792a63c1d86e9bca8fb9b524a811f11ff048997`
Final SHA: `e8d57eed0c4b39bd651b7393e7af25b5a10a7558`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewer: `b01_architecture_review`

## Scope delivered

Parent requirement IDs: PLAT-01, WF-01, BACK-01, WS-10, B-01.

The existing `marklab` package is now the implicit root/default member of a non-virtual Cargo workspace using resolver 2 and shared package metadata. `fuzz/` remains explicitly excluded and independently resolvable. Empty project/workflow packages were rejected; B-04 must create them with their first behavior and callers.

No scientific, numerical, public API, CLI, config, result, output, CI, dependency, registry lock, or fuzz-manifest behavior changed.

## Changed files

- `Cargo.toml`: root workspace, resolver, shared package metadata, and fuzz exclusion.
- `tests/workspace_contract.rs`: Cargo-observed member/default/target/safety-boundary contract.
- `docs/implementation/**`: task, ownership, decision, status, requirement, and validation records.

## Behavior-first evidence

1. The first workspace contract exited 101 because the root had no `[workspace]`.
2. Intermediate red assertions rejected missing fuzz exclusion, premature ceremonial members, and explicit root membership.
3. Direct fuzz metadata exited 101 with explicit `members = ["."]` because Cargo treated the excluded nested package as belonging to the root workspace.
4. The final implicit-root design passes both root and standalone-fuzz metadata with no lockfile delta.

## Commands actually run

| Command | Result |
|---|---|
| `cargo +1.96.0 test --locked --test workspace_contract` | 1 passed, 0 failed |
| `cargo +1.96.0 test --locked --test workflow_contract --test api_contract` | 12 passed, 0 failed |
| `cargo +1.96.0 check --locked --workspace --all-targets --all-features` | pass |
| `cargo +1.96.0 metadata --locked --format-version 1 --no-deps` | pass; one root/default `marklab` package |
| `cargo +1.96.0 metadata --locked --format-version 1 --no-deps --manifest-path fuzz/Cargo.toml` | pass; standalone fuzz workspace |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | pass |
| `cargo +1.96.0 fmt --all --check` | pass |
| `cargo +nightly fuzz check` | pass |
| `cargo +1.96.0 package --locked` | pass at clean final SHA; 242 files, 1.7 MiB/389.6 KiB compressed; verification build passed |
| `git diff --check` plus targeted protected-file diff | pass; no protected delta |

## Review disposition

The read-only reviewer confirmed the standalone fuzz boundary and no unintended lock delta, recommended deferring empty packages until B-04, and recommended Cargo-observed rather than TOML-only assertions. All recommendations were applied. The reviewer edited no files; its task-owned semantic server was stopped.

## Known risks and remaining work

- The implicit-root layout is a deliberate Cargo 1.96 workaround for the nested-exclusion behavior; `workspace_contract` and independent fuzz metadata guard it.
- B-01 did not prove every CLI/result fixture; B-02 owns full compatibility characterization.
- B-03 owns workspace-wide CI/policy and dependency-direction gates.
- B-04 owns the first project/workflow packages and real vertical slice.

## Next exact action

```bash
cargo +1.96.0 test --locked --test api_contract --test cli --test result_v03
```

Then write B-02's task contract from the observed parity surface before any facade change.

## Scope audit

- Unrelated files changed: no.
- Dependency or lockfile changes: no.
- Production source or algorithm changes: no.
- Result/config/CLI/schema changes: no.
- Duplicate canonical implementation introduced: no.
- Unsupported claim introduced: no.
- Additional worktree created: no.
