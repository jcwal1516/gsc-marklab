# Task Handoff — B-02

Status: complete
Base SHA: `e8d57eed0c4b39bd651b7393e7af25b5a10a7558`
Final SHA: `f1bcc94d4a5f96825fae32676630304f31d332ea`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewer: `b02_compatibility_audit`

## Scope delivered

Parent requirement IDs: PLAT-01, WS-10, B-02.

The existing implicit root `marklab` package was proven to be the compatibility shell: private implementation modules sit behind the crate-root re-export facade, and the configured binary delegates only to `marklab::run_cli()`. No source move or duplicate package was justified.

The existing marked CLI fixture now proves exact direct-library/CLI result-core equality for identical cells, mask, and config, excluding execution timing telemetry. Multimodal parity already had the equivalent assertion.

## Changed files and symbols

- `tests/cli.rs`: marked direct-library/CLI result-core parity assertion.
- `docs/implementation/**`: B-01 closure plus B-02 task, ownership, decision, status, requirement, validation, and handoff records.
- Production canonical symbols changed: none.

## Commands actually run

| Command | Result |
|---|---|
| `cargo +1.96.0 test --locked --test cli analyze_cli_writes_result_json_from_csv_and_geojson_mask` | 1 passed |
| `cargo +1.96.0 test --locked --test api_contract --test cli --test config_v02 --test result_v03 --test multimodal_cli` | 65 passed |
| `cargo +1.96.0 test --locked --features wsi,cli --test wsi_integration --test cli` | 26 passed; 1 external oracle ignored |
| `cargo +1.96.0 test --locked --no-default-features --features cli --test cli --test multimodal_cli` | Reviewer: 35 passed; existing cfg-only unused-import warnings recorded for B-03 |
| `cargo +1.96.0 test --locked --lib output::tests` | Reviewer: 16 passed |
| `cargo +1.96.0 check --locked --no-default-features` | pass |
| `cargo +1.96.0 test --locked --test workflow_contract --test workspace_contract` | 7 passed |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | pass |
| `cargo +1.96.0 fmt --all --check`; `git diff --check` | pass |
| `git diff --exit-code 55fce12f10684a9081ca1f744f87d6f5feedcb24..f1bcc94d4a5f96825fae32676630304f31d332ea -- src Cargo.lock fuzz .github` | Reviewer: pass; B-01/B-02 changed no production, lock, fuzz, or workflow compatibility surface |

## Tests not run and harness limitation

- The public Aperio/OpenSlide oracle remained ignored locally because its checksummed external fixture/oracle was not present; the scheduled workflow retains it.
- Default and WSI `assert_cmd::cargo_bin` suites must be serial in one target directory. An initial concurrent run let the default test observe the WSI-enabled binary; isolated reruns passed completely.

## Decisions and risks

- DEC-0011 recognizes the existing root facade as the shell and rejects an unmotivated source move.
- `cli` without default features passes but exposed cfg-specific unused imports in `src/cli/batch.rs`; B-03 owns the warning fix and feature-matrix gate.
- Before B-04 adds members, B-03 must make workspace-wide commands explicit and prevent upward local dependencies/CLI-gated core science.

## Remaining work and next exact action

```bash
cargo +1.96.0 test --locked --test workflow_contract workspace_policy_is_explicit
```

Add the B-03 policy assertion first and confirm it fails against the pre-B-03 workflows.

## Scope audit

- Unrelated files changed: no.
- Production, dependency, lockfile, feature, API, CLI, config, result, artifact, and schema changes: no.
- Duplicate implementation or unsupported claim: no.
- Additional worktree or remote mutation: no.
