# Task Handoff — A-02

Status: complete
Base SHA: `55fce12f10684a9081ca1f744f87d6f5feedcb24`
Final SHA: `fc986c0c4e06216cc85d55d2315482a0107bf5b7`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `a02_baseline` (read-only runner), integrated by `/root`

## Scope delivered

Parent requirement IDs: WS-00, WS-01, A-02

Scientific behavior: reproduced the current executable correctness, feature, WSI, dependency, package, fuzz-build, heap, benchmark, and deterministic smoke baseline.

Explicit non-goals preserved: no tracked edits, dependency installation, weakened command, ignored failure, or formal-calibration claim.

## Changed files

- Runner: none.
- Lead integration: validation/performance/status ledgers and this handoff only.

## Changed canonical symbols

- None. Registry updated: no symbol change.

## Commands actually run

| Command | Exit/result | Tests run/passed/skipped | Notes |
|---|---|---|---|
| `cargo +1.96.0 fmt --all --check` | 0 | formatting | 0.119 s |
| `cargo +1.96.0 clippy --locked --all-targets --all-features -- -D warnings` | 0 | all targets/features | 11.315 s |
| `cargo +1.96.0 nextest run --locked --all-features` | 0 | 402/402 passed; 22 skipped; 1 slow | 63.528 s test summary; ~90 s with build |
| `cargo +1.96.0 test --locked --doc --all-features` | 0 | 0/0/0 | 0.354 s; no doctests exist |
| `cargo +1.96.0 check --locked --no-default-features` | 0 | compile | 5.840 s |
| `cargo +1.96.0 test --locked --features wsi,cli --test wsi_integration` | 0 | 10 passed; 1 ignored | External Aperio fixture remains scheduled |
| `cargo audit` | 0 | 334 dependencies; 1,225 advisories | No vulnerabilities; two reviewed unmaintained warnings |
| `cargo deny check advisories licenses bans sources` | 0 | four checks | Duplicate-version warnings recorded |
| `cargo machete` | 0 | no unused dependencies | under 0.1 s |
| `cargo package --locked` before bootstrap commit | 101 | no package built | Expected refusal for 12 uncommitted WS-A files; no `--allow-dirty` |
| `cargo package --locked` at `fc986c0` | 0 | 237 files packaged and verified | 1.7 MiB/381.0 KiB compressed; 15.6 s |
| `cargo +nightly fuzz check` | 0 | all declared targets built | 13.510 s |
| `cargo +1.96.0 test --locked --no-default-features --features dhat-heap --lib dhat_ -- --test-threads=1` | 0 | 3 passed; 180 filtered | 14 narrow-feature compile warnings; 11.944 s |
| `env MARKLAB_BENCH_PROFILE=smoke cargo +1.96.0 bench --locked --all-features -- --quick` | 0 | 8 workloads | Plotters fallback because Gnuplot unavailable; ~2m44s |
| `mktemp -d /tmp/marklab-a02-smoke.XXXXXX` | 0 | created `/tmp/marklab-a02-smoke.xEna92` | Outside repository |
| `cargo +1.96.0 run --release --locked --features wsi --bin marklab -- smoke --suite synthetic --replicates 10 --out /tmp/marklab-a02-smoke.xEna92` | 0 | 12/12 scenarios; 120/120 replicates; 0 failed | `smoke.json` 14,043 bytes; ~1m34s |
| `git diff --check` | 0 | scope | Runner made no tracked edit |

## Tests not run

- Scheduled 1,000-replicate calibration: not part of the requested baseline gate set and intentionally outside PR/main smoke; historical evidence only in this task.
- Public external Aperio WSI oracle: ignored because its checksummed download/oracle environment was not supplied locally; local 10-test WSI matrix passed.
- Full scheduled Criterion suite: smoke profile ran; weekly full profile not run.

## Numerical/scientific evidence

- Oracle: existing checked-in fixtures, including ERL and WSI local oracles, executed through the current production paths.
- Simulation/calibration: quick deterministic smoke only; 12 scenarios × 10 replicates, all complete. This is not formal calibration.
- Determinism: current strict-repro/thread tests included in 402 passing tests.
- Undefined-state coverage: current result/engine suites included; no new method.

## Performance evidence

Host: macOS 26.5.2 arm64, Apple M4 Pro, 48 GiB, 12 logical CPUs; Rust 1.96.0/LLVM 22.1.2.

| Workload | Criterion interval |
|---|---:|
| Multiscale residual | 45.703–45.746 ms |
| CSV load | 9.0462–9.2575 ms |
| CSV decode/filter | 2.7986–2.8557 ms |
| Indexed nearest neighbor | 6.2113–6.2404 ms |
| Periodogram | 11.657–11.775 ms |
| Permutations | 15.140–15.168 ms |
| ERL | 15.300–15.313 ms |
| Structure factor | 56.196–56.479 ms |

Assessment: reproducible smoke baseline only; no before/after optimization claim.

## API/config/CLI/schema/artifact changes

None.

## Assumptions

1. Ignored scheduled/external tests remain explicit and are not counted as passes.

## Decisions

- DEC-0004: architecture changes remained gated on this baseline.

## Known risks

1. Formal calibration covers broader claims only when the scheduled job is rerun.
2. DHAT's narrow feature combination emits warnings not exercised under `-D warnings`; all-feature Clippy passed.
3. Two reviewed unmaintained transitive packages remain.

## Remaining work

1. Preserve these gates through workspace migration and rerun them at WS-B closure.

## Next exact action

```bash
git status --short
```

Then B-01 behavior-first workspace characterization.

## Scope audit

- Unrelated files changed: no.
- Dependency changes: no.
- Formatting sweep: no.
- Hidden result-format change: no.
- Duplicate canonical implementation introduced: no.
- Unsupported claim introduced: no.
- Worktree status: runner ended with only lead-owned WS-A documentation dirty; package rerun passed on clean `fc986c0`.
