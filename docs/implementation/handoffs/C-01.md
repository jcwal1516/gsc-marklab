# Task Handoff — C-01 Typed Identities and Cohort Hierarchy

Status: complete
Base SHA: `3ea2e9849ae024759d871d1e6ce6a566d2c40d92`
Contract SHA: `a7dc67bcb7da94867e784e5cf08668b8240e2e57`
Final SHA: `a1260445a383c59381b2c7c7881cebb10e10c156`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewer: `c01_identity_audit`

## Scope delivered

Parent requirement IDs: DATA-01, FND-01, COH-01, WS-20, C-01.

- Added leaf `marklab-core` with exact opaque typed IDs for site, patient, timepoint, specimen, block, slide, section, core, region, cell, and patch. Values retain their typed kind, allow identical text across kinds, and reject blank, surrounding-whitespace, control-character, and over-255-byte inputs.
- Added `marklab-data` with immutable nodes, enumerated containment, independently declared biological sources, deterministic validation errors, explicit repeated-measure sets, nearest biological resolution, bounded enclosing biological lineage, and factual design summaries.
- Enforced the anti-pseudoreplication role matrix: only patient/specimen objects may be biological units; site/timepoint/cell/patch are structural; lower physical objects can be subsamples, technical replicates, or structural where allowed.
- Represented a donor TMA core as a biological subsample sourced to its donor unit while contained by a recipient slide. Structural descendants inherit that source without reading ID text.
- Preserved nested patient/specimen levels: nearest lookup returns the specimen, while `belongs_to_biological_unit` and repeated-set validation can select the enclosing patient explicitly. Core/region summaries count each declared level factually and do not select an inferential unit.
- Added one non-overwriting hierarchy slot to `MarklabProject`; no persistent schema, root public re-export, current input adapter, configuration/result change, or scientific algorithm was added.
- Added the declared 10,000-cell smoke and 1,000,000-cell/10,000-specimen correctness-checked Criterion workloads.

## Changed files

- `crates/marklab-core/**`: `HierarchyKind`, `HierarchyId`, eleven ID newtypes, `IdentityError`.
- `crates/marklab-data/**`: `HierarchyNode`, `ReplicationRole`, `ReplicationRoleKind`, `RepeatedMeasureSet`, `CohortHierarchy`, `CohortDesignSummary`, and typed errors.
- `crates/marklab-project/**`: optional installed `CohortHierarchy`, immutable accessor, `HierarchyAlreadyInstalled`.
- Root/fuzz manifests and locks: two local package records/edges only; root uses data as a test/benchmark dependency.
- `tests/data_hierarchy.rs`, workspace/workflow contracts, and `benches/cohort_hierarchy.rs`.
- C-01 implementation ledgers, decision, interface contract, and handoff.

## Changed canonical symbols

- Added the eleven typed ID newtypes, `HierarchyKind`, `HierarchyId`, and `IdentityError` in leaf core.
- Added `HierarchyNode`, `ReplicationRole`, `ReplicationRoleKind`, `RepeatedMeasureSet`, `CohortHierarchy`, `CohortDesignSummary`, and their typed errors in data.
- Added the project hierarchy slot/accessors and `ProjectError::HierarchyAlreadyInstalled`.
- Registry updated: yes, in `CANONICAL_SYMBOLS.md`; no competing implementation was found.

## Behavior-first evidence

| Behavior | Red evidence | Final evidence |
|---|---|---|
| Workspace and API boundary | Workspace test rejected absent members; hierarchy integration failed to resolve the new crate/API | Five-layer metadata contract and all 14 hierarchy scenarios pass |
| Role-kind safety | Compile failed on missing `ReplicationRoleKind`/`UnsupportedReplicationRole` | Illegal biological site/timepoint/block/slide/section/core/region/cell/patch declarations return the typed error before source validation |
| Repeated-set uniqueness | Earlier implementation allowed multiple disjoint same-kind sets for one unit | One set per `(biological unit, observation kind)`; a specimen set and core set may coexist, while same-kind duplication and global member reuse are typed failures |
| Nested biological levels | Patient-level specimen pair failed with `ObservationDoesNotResolve`; membership API was absent | Nearest specimen lookup, enclosing patient membership, patient pair, and lineage-expanded core/region summaries all pass |
| Adversarial depth | No deep regression existed | 10,000-node chain resolves without recursion and a 10,000-node tail into a 100-node cycle fails deterministically |
| Independent fuzz lock | Locked standalone check exited 101 on stale resolution | Offline regeneration added local records/edges only; locked and nightly fuzz checks pass |

## Commands actually run

| Command | Result |
|---|---|
| `cargo +1.96.0 test --locked --test data_hierarchy` | 14 passed |
| `cargo +1.96.0 test --locked -p marklab-core -p marklab-data -p marklab-project` | 6 unit tests passed across three packages; doc tests passed |
| `cargo +1.96.0 test --locked --test workspace_contract --test workflow_contract --test api_contract` | 15 passed |
| `cargo +1.96.0 nextest run --locked --workspace --all-features` | 430 passed, 22 skipped, 1 slow |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | pass |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --no-default-features --features cli -- -D warnings` | pass |
| `RUSTDOCFLAGS='-D missing-docs' cargo +1.96.0 doc --locked --no-deps -p marklab-core -p marklab-data -p marklab-project` | pass |
| Root and standalone-fuzz locked metadata; `cargo +nightly fuzz check` | pass |
| `cargo audit`; `cargo deny check advisories licenses bans sources`; `cargo machete` | pass with the same reviewed audit/duplicate warnings; no unused dependency |
| Smoke/full `cohort_hierarchy` Criterion commands | 1.1645–1.1726 ms for 10,000 cells; 147.13–147.27 ms for 1,000,000 cells |
| `cargo +1.96.0 fmt --all --check`; `git diff --check`; lock provenance assertion | pass |
| Root-source diff plus duplicate-formula/field-policy search over the new crates | pass; root `src/**` unchanged and no current formula/config/result/writer owner name occurs in core/data |
| `cargo +1.96.0 package --locked --workspace` at clean implementation SHA | all five archives verification-compiled |

## Tests not run

- The eight-row feature compile matrix, dedicated WSI integration, DHAT heap regression, and release synthetic smoke were not repeated for this focused task. C-01 changes no root feature, WSI, algorithm, output, or CLI path; the full all-feature workspace suite and both mandated Clippy configurations pass. Remaining compatibility risk is low and the full matrix remains a WS-C exit gate.
- No 10-million-cell hierarchy run or peak-allocation profiler was executed. The contract explicitly selected the permitted 1-million-cell fallback; memory behavior beyond retained O(objects) structures remains unmeasured.
- No real-data hierarchy ingest or CellViT row-alignment validation ran because C-03/C-04 schemas and stable row identity are prerequisites. This prevents, rather than weakens, any real-data claim.
- Local Markdown and GitHub Actions semantic linters remain unavailable. No workflow changed; Markdown source/diff checks are not claimed equivalent to those tools.

## Numerical/scientific evidence

- Oracle/tolerance: structural invariants use exact typed equality and count assertions; no numerical tolerance applies.
- Simulation/calibration/real data: none introduced or claimed. The TMA, nested patient/specimen, repeated, multisite, multicore, and multiregion fixtures are declarative contract examples, not biological validation.
- Determinism: input-order error selection, cycle order, exact summaries, and typed source/link failures are regression-tested.
- Undefined-state coverage: missing/non-biological sources and units, unresolved terminal branches, missing/reused observations, unsupported roles/nesting, and absent membership IDs all have explicit outcomes.

## Performance evidence

- Hardware/toolchain: Apple M4 Pro, 48 GiB, macOS 26.5.2 arm64; Rust/Cargo 1.96.0, LLVM 22.1.2.
- Workload: correctness-checked construction from declarative nodes, 100 cells per specimen; 10,000-cell smoke and 1,000,000-cell full fallback.
- Result: 1.1645–1.1726 ms smoke and 147.13–147.27 ms full. There is no pre-C-01 baseline because the hierarchy did not exist; Criterion detected no change between local implementation refinements.
- Peak memory/allocations: not measured; retained design is O(objects), and no peak-memory claim is approved.

## API/config/CLI/schema/artifact changes

- `IC-0008` is infrastructure only. It provides explicit study-design facts and prevents cell/patch pseudoreplication; it does not establish a randomization unit or population-level result.
- Repeated-member order is retained for deterministic declaration/iteration only. It has no chronological or condition meaning; C-02/C-03 metadata must carry those semantics explicitly.
- `SiteId` currently means cohort/enrollment site only. Collection, assay, processing, and scanning sites require later typed provenance rather than reuse of that relation.
- C-01 retains no serialized hierarchy and performs no cross-artifact drift check. C-03 owns versioned manifests, digests, migrations, path/object-store security, and result-format 0.4 integration.
- Current compatibility `Pattern`/multimodal cell strings are unchanged. C-04 must prove deterministic `CellId` alignment before importing the remote CellViT rows; filenames or positional `.pt` linkage are insufficient.
- The role matrix bounds biological lineage to specimen → patient. Adding another biological level is a future reviewed API change, not an inferred extension.
- The 1-million-cell measurement validates hierarchy construction only. It is not a vector-table, memory-map, persistence, or 10-million-cell benchmark.

## Assumptions

1. ID uniqueness is scoped to typed kind and one analysis bundle; opaque text has no path, parent, time, or source meaning.
2. `SiteId` is cohort/enrollment context in C-01, and patient/specimen are the only currently supported biological-unit levels.
3. A declared biological source is authoritative study metadata supplied at a trusted boundary; C-01 validates consistency but does not discover it.

## Decisions

- `DEC-0015`: containment and biological source are independent so multi-donor TMA structure remains truthful.
- `DEC-0016`: biological ancestry is bounded to specimen → patient while nearest resolution remains specific and inferential level selection remains downstream.

## Known risks

1. Persistence and cross-artifact drift are absent until C-03; in-memory validity alone cannot prove a durable project remained unchanged.
2. Current compatibility cell records are not yet mapped to the new `CellId`; premature remote vector import could still misalign rows.
3. Collection/processing/scanning-site provenance and temporal/condition semantics need dedicated later metadata rather than reuse of C-01 fields.

## Independent review and structural judgment

The reviewer first identified TMA role ambiguity, unrestricted biological roles, over/under-restrictive repeated-set identity, missing adversarial cases, a stale fuzz lock, and nested-level shadowing. Each material finding received a regression and was corrected. The final re-review found no material issue and confirmed bounded-linear ancestry. The 916-line hierarchy module was reviewed as one cohesive ordered validation pipeline whose stages share indexed graph state and deterministic error precedence; it was not split merely for line count. No unsafe code, recursive traversal, new registry package, remote access, or reviewer edit was introduced.

## Remaining work

1. C-02: typed units, 2-D/3-D coordinate frames, transform chains, uncertainty references, and serial-section metadata.
2. C-03/C-04: durable schemas, cross-artifact integrity, and proven CellViT row linkage.
3. FND-06: explicit inferential/randomization-unit selection using this hierarchy.

## Next exact action

Freeze the C-02 contract against current registration types and remote physical-scale evidence, then add a failing `tests/coordinate_frames.rs` boundary suite before production edits.

## Scope audit

- Existing formulas, config 0.2, result 0.3, `OutputWriter`, CLI, CI, and WSI behavior changed: no.
- Root stable hierarchy re-export or current adapter migration introduced: no.
- Persistent schema/serde, artifact-table implementation, remote patient data, or filename inference introduced: no.
- Registry package/version/checksum delta: no; only local package records/edges changed in root/fuzz locks.
- Additional worktree, remote mutation, dependency install, publish, deploy, push, or secret persistence: no.
