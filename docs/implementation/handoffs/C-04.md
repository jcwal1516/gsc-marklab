# Task Handoff — C-04 CellViT Embedding Table and Safe Source-Bundle Import

Status: complete
Base SHA: `0c00e8b0f3984fdbcae8e0d5962a5b2c421c8913`
Final SHA: `55d1c8b8a07b2dbf40f77b5590127456f9042295`
Contract SHA: `dd834a7`
Implementation SHAs: `8b90141`, `c6cd81e`, `ae8d3fa`, `d3459d4`, `56026f5`, `ff15612`, `49ed113`, `d4c10f8`, `48d2637`, `ef5a8cf`, and `55d1c8b`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewers: `c04_embedding_audit`, `c04_feasibility_audit`

## Scope delivered

Parent requirement IDs: DATA-01, EMB-CORE, FND-05, WS-24, C-04.

Scientific behavior: none. C-04 validates embedding storage, identity, validity, provenance, physical encoding, and resource invariants; its benchmark kernels are integrity checks rather than estimands.

Explicit non-goals preserved: no embedding inference/statistic/classifier/predictor, patch link, model backend, patient-level analysis, result/config/CLI change, biological claim, real-corpus promotion, unsafe/mmap/zero-copy claim, or executable model-artifact deserialization.

- Added the layer-3 `marklab-embeddings` package with one canonical contiguous single-cell embedding table keyed by the existing typed `CellId`. Exact extraction validity distinguishes present, missing, failed, and QC-rejected rows; public row/block APIs never expose private fillers.
- Added bounded canonical expected-cell, source-identity-map, spatial-context, row-link, and provenance schemas. Exact logical digests, artifact IDs, tensor dimension, and source/graph dependencies are cross-bound by an unforgeable verified graph before a source candidate becomes a table.
- Added a closed non-pickle CellViT source adapter for exact little-endian C-order NPY `f32` plus the frozen CSV bundle profile. Borrowed, chunked, and managed paths reject coercion, positional identity, malformed/truncated/oversized input, nonfinite values, row drift, and privacy-unsafe error leakage.
- Added canonical embedding-table and row-link Arrow IPC and Parquet profiles with deterministic writers, bounded raw preflight before Arrow/Parquet stock decode, exact physical metadata, managed publication/readback, one-row-group confinement, integrity precedence, logical parity, and hostile-input coverage.
- Added status-safe block access, one constant-state QC/logical-digest accumulator, and bounded Arrow/Parquet scans that retain one charged batch or row group instead of a materialized table.
- Added seven-route structured fuzz coverage, shared source/domain/Arrow/Parquet differentials, immutable checksum goldens, a 10,000 × 1,280 Criterion/DHAT smoke, and a 1,000,000 × 256 fixed-order scale workload.
- Reconciled all 32 authorized Schürch NPY/CSV/manifest bundles read-only: 60,191 finite/QC-pass rows, width 1,280, and both pinned aggregate digests. The aggregate report is deliberately non-promotable and names four exact missing promotion fields.
- Preserved result 0.3, config 0.2, CLI behavior, current CellViT class-label CSV loading, compatibility Arrow/Parquet IO, `OutputWriter`, and every existing scientific method. No WSI/source object was modified and no `.pt`, `.pth`, pickle, or checkpoint was deserialized.

C-04 completes the bounded cell-level embedding substrate and EMB-CORE. It does **not** close FND-05, WS-24, DATA-01, or WS-C: C-05 owns patch/region/slide vectors and links, C-06 owns general measurement status, WS-25 owns broad interchange, and WS-50/WS-52 own embedding science and patient-held-out prediction.

## Changed files

- `crates/marklab-embeddings/**`: `CellEmbeddingTable`, `EmbeddingStatus`, status-aware row/block views, `ExpectedCellSet`, `CellIdentityMap`, `EmbeddingSpatialContext`, `CellEmbeddingRowLink`, `CellEmbeddingProvenance`, verified artifact receipts/graph, source parsing/reconciliation, QC, Arrow/Parquet codecs, and typed errors/budgets.
- `crates/marklab-project/**`: `ArtifactReadSeek`, scoped verified-reader callbacks, send-capable publication, and location-free fresh artifact drafts used by physical codecs.
- Root facade/manifests/locks: downward package dependency, exact feature propagation, prerequisite re-exports, one Criterion target, and one DHAT integration gate.
- `tests/cellvit_*`, `tests/cell_embedding_*`, and package tests: domain/provenance/source/store/physical/differential/resource/adversarial/real-reconciliation coverage.
- `fuzz/fuzz_targets/embedding_inputs.rs` and the independently locked fuzz manifest/lock: seven bounded input routes using only registry tuples already reviewed in the root lock.
- `docs/implementation/**`: task contract, DEC-0022 through DEC-0031, interface/claim/symbol/requirements/validation/performance/status records, and this handoff.

## Changed canonical symbols

- `CellEmbeddingTable`, `CellEmbeddingRow`, `CellEmbeddingView`, `CellEmbeddingBlock`, `EmbeddingStatus`, and `EmbeddingQcSummary`: absent → exact contiguous/status-safe logical table and scan contract.
- `ExpectedCellSet`, `CellIdentityMap`, `EmbeddingSpatialContext`, `CellEmbeddingRowLink`, and `CellEmbeddingProvenance`: absent → bounded canonical value/artifact schemas with exact logical identities.
- `CellVitHeImportCandidate`, `SourceBundleReconciler`, and their reports/errors/budgets: absent → closed non-pickle source adaptation and aggregate-only reconciliation boundary.
- `CellEmbeddingArtifact`, physical receipts/bindings, and Arrow/Parquet table/row-link APIs: absent → verified graph-bound canonical physical replicas, scans, and publications.
- `ArtifactReadSeek`, verified-reader callbacks, `publish_send`, and fresh-artifact drafts: existing store lacked required scoped seek/send/fresh behavior → focused additive capability-confined IO used by C-04.

Registry updated: yes. No competing embedding-table, source-profile, or physical embedding-codec owner was found.

## Behavior-first and review evidence

| Boundary | Red/review finding | Final behavior |
|---|---|---|
| Domain identity and validity | Table/status/expected/map/context types were absent | Exact typed construction, fixed goldens, signed-zero canonicalization, status/vector invariants, by-value context validation, and aggregate-only `Debug` pass |
| Provenance and promotion | Initial one-shot design could bind values to incomplete or unrelated provenance | Source adaptation returns a non-promotable candidate; exact row-link publication and verified graph binding are required before finalization |
| Store IO and publication | Existing store API could not safely lend one verified seekable descriptor or publish send-bound writers | Pre/post verified scoped readers and failure-atomic fresh/send publication pass integrity, concurrency, redaction, and compatibility tests |
| Arrow/Parquet safety | Stock decode could allocate from hostile declarations; initial accounting omitted some coexisting decoded buffers | Footer/Message and compact-Thrift/page preflight reject hostile sizes/layouts first; exact/one-short retained/decoded/file/row-group gates and selected-middle confinement pass |
| Logical parity | Physical formats and source import initially lacked one shared oracle | Fixed, status-complete, generated, and property cases agree across source/domain/block/Arrow/Parquet rows, QC counts, and logical digest |
| Scale and memory | Benchmark selection/arithmetic/checksum/RSS ownership and DHAT publication scope were underspecified | DEC-0030/0031 freeze both profiles, generator, all 4,096 random accesses, reductions, goldens, runtime RSS, and fresh disk-backed DHAT writes |
| Phase integration | First all-feature Nextest panicked because the benchmark contract read `benches/support/` as a file; all-feature Clippy then exposed DHAT helpers under simultaneous mimalloc | Recursive Rust-only benchmark discovery and allocator-consistent cfg predicates pass focused regression, 612-test Nextest, matrix, and both warning-denied Clippy gates |
| Real reconciliation | Remote assets could not be promoted safely from length/order assumptions | Explicit source rows and identity-map rules reconcile exactly 32 allowlisted bundles without copying identifiers/values into evidence or claiming promotion |

## Commands actually run

| Command | Exit/result | Tests run/passed/skipped | Notes |
|---|---|---|---|
| `cargo +1.96.0 nextest run --locked --workspace --all-features` | 0 | 612/612 passed; 23 skipped; 1 slow | Final rerun after recursive benchmark-contract fix; 64.529 s summary |
| `cargo +1.96.0 test --locked --test cellvit_embedding_table` | 0 | 6/6 passed | Exact domain table contract |
| `cargo +1.96.0 test --locked -p marklab-embeddings` | 0 | 8/8 passed; 0 direct hostile tests without Parquet feature | Exact default-feature package command |
| `cargo +1.96.0 test --locked -p marklab-project` | 0 | 41/41 passed | Unit/catalog/store/verified-IO suites |
| `cargo +1.96.0 test --locked --test project_workflow --test api_contract --test result_v03 --test workspace_contract` | 0 | 28/28 passed | Compatibility and architecture boundaries |
| `RUSTDOCFLAGS='-D missing-docs' cargo +1.96.0 doc --locked --no-deps -p marklab-embeddings -p marklab-project` | 0 | documentation build | Missing public docs denied |
| `cargo +1.96.0 test --locked --workspace --doc --all-features` | 0 | six doc-test binaries; 0 executable doc tests | Workspace documentation gate |
| `cargo +1.96.0 check --locked --workspace --all-targets` | 0 | compile | Default row, clean |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features` | 0 | compile | Minimal row; 14 documented instrumentation warnings |
| `cargo +1.96.0 check --locked --workspace --all-targets --all-features` | 0 | compile | Aggregate row, clean after DHAT/mimalloc cfg fix |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features --features csv` | 0 | compile | CSV-only row; documented instrumentation warnings |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features --features parquet` | 0 | compile | Parquet-only row; documented instrumentation plus four existing compatibility-writer warnings |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features --features cli` | 0 | compile | CLI-only row, clean |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features --features wsi` | 0 | compile | WSI-library row; documented instrumentation warnings |
| `cargo +1.96.0 check --locked --workspace --all-targets --no-default-features --features wsi,cli` | 0 | compile | WSI+CLI row, clean |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | 0 | all targets/features | Initial phase run exposed five DHAT-helper dead-code errors; final exact rerun passes |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --no-default-features --features cli -- -D warnings` | 0 | all CLI-only targets | Pass |
| `cargo +1.96.0 test --locked --package marklab --features wsi,cli --test wsi_integration` | 0 | 10 passed; 1 ignored | Checksummed public Aperio/OpenSlide oracle remains scheduled |
| `cargo +nightly fuzz check` | 0 | all declared fuzz targets built | 3.19 s; root, embedding package, and standalone fuzz package |
| `cargo audit` | 0 | 370 dependencies / 1,225 advisories | No vulnerabilities; two policy-allowed unmaintained warnings |
| `cargo deny check advisories licenses bans sources` | 0 | four policy categories | All pass; 17 configured duplicate-version warnings |
| `cargo machete` | 0 | dependency scan | No unused dependencies |
| `cargo +1.96.0 test --locked --package marklab --no-default-features --features dhat-heap --lib dhat_ -- --test-threads=1` | 0 | 3/3 passed; 180 filtered | Legacy heap gate; 14 documented narrow warnings |
| `cargo +1.96.0 test --locked --features csv,parquet,dhat-heap --test cellvit_embedding_heap -- --exact embedding_10k_1280_peak` | 0 | 1/1 passed | Final phase rerun 138.51 s |
| `cargo +1.96.0 test --locked --features csv,parquet,dhat-heap --test cellvit_embedding_heap -- --exact embedding_10k_1280_peak --nocapture` | 0 | 1/1 passed | Diagnostic checkpoint 136.94 s; current 0, peak 324,194,945, cap 603,979,776 bytes |
| `env MARKLAB_BENCH_PROFILE=smoke cargo +1.96.0 bench --locked --workspace --all-features -- --quick` | 0 | 8 targets / 10 intervals | Embedding 4.1952–4.2284 s; all existing targets reported no detected regression |
| `cargo +1.96.0 bench --locked --bench cell_embeddings --features csv,parquet --no-run` | 0 | release build | Current binary built outside timed RSS gate |
| `/usr/bin/time -l cargo +1.96.0 bench --locked --bench cell_embeddings --features csv,parquet -- '1m_x_256' --noplot` | 0 | 10 checked samples | 10.446–10.488 s; 24.408–24.506 million values/s; RSS 1,117,552,640 bytes |
| `cargo +1.96.0 run --release --locked --package marklab --features wsi --bin marklab -- smoke --suite synthetic --replicates 10 --out /tmp/marklab-c04-smoke.cdxpkS` | 0 | 12/12 scenarios; 120/120 replicates; 0 failed | 14,043-byte output validated and moved to Trash |
| `MARKLAB_AUTHORIZED_CELLVIT_ROOT=/tmp/marklab-c04-authorized.g8Z0iv cargo +1.96.0 test --locked -p marklab --features csv --test authorized_cellvit_reconciliation -- --ignored --exact reconciles_all_authorized_bundles` | 0 | 1/1 ignored test selected and passed | 16.47 s; 32 / 60,191 / 1,280 and both pinned digests; stage moved to Trash |
| `cargo +1.96.0 package --locked --workspace` | 101, expected blocker | six archives created; core verified; data verification failed | Published pre-C-02 core lacks current coordinate IDs; DEC-0018 remains release-blocking and this gate is not green |
| `cargo +1.96.0 package --locked --workspace --config 'patch.crates-io.marklab-core.path="crates/marklab-core"' --config 'patch.crates-io.marklab-data.path="crates/marklab-data"' --config 'patch.crates-io.marklab-project.path="crates/marklab-project"' --config 'patch.crates-io.marklab-embeddings.path="crates/marklab-embeddings"' --config 'patch.crates-io.marklab-workflow.path="crates/marklab-workflow"'` | 0 | all six archives verified | Supplemental source compatibility only; not registry resolvability |
| `cargo +1.96.0 fmt --all --check` | 0 | formatting | Pass |
| `git diff --check` | 0 | whitespace/scope | Pass |

## Package and release boundary

The exact clean `cargo +1.96.0 package --locked --workspace` created all six archives and verified `marklab-core`, then exited 101 while verifying normalized `marklab-data` against the published pre-C-02 `marklab-core 0.1.0`, which lacks current coordinate IDs. This is the existing DEC-0018 release blocker; it is not a C-04 correctness failure and is not called green.

The supplemental command with ephemeral CLI `patch.crates-io` overrides for `marklab-core`, `marklab-data`, `marklab-project`, `marklab-embeddings`, and `marklab-workflow` verified every archive. It proves current archive-source compatibility only, not external registry resolvability. No permanent patch, version change, publication, `--no-verify`, or CI weakening was introduced.

## Numerical/scientific evidence

- Oracle: independent framed logical-digest/QC reference cases, bit-exact NPY/CSV/domain/Arrow/Parquet differentials, immutable smoke/full logical and numeric goldens, and exact authorized aggregate/reconciliation digests.
- Tolerance: schema, identity, counts, statuses, source rows, dimensions, `f32` component bits, digests, and deterministic writer bytes use exact equality. The benchmark reductions are fixed-order `f64::to_bits`; no floating tolerance or platform-dependent parallel reduction is accepted.
- Simulation/calibration: deterministic synthetic storage workloads only. No type-I error, power, coverage, classifier calibration, or embedding-science claim is introduced by C-04.
- Real-data validation: 32 authorized bundles reconcile to exactly 60,191 finite/QC-pass rows at width 1,280. This is aggregate source-profile evidence, not a promoted canonical real-patient table.
- Determinism: source bundle order, chunk partitions, physical format, fresh processes, Criterion profile selection, LCG access order, arithmetic order, and checksum framing are regression-bound.
- Undefined-state coverage: every extraction status, missing/failed/rejected vector, all-zero present vector, malformed identity/context/provenance/link, truncated/hostile physical input, integrity failure, and resource-category boundary has an explicit tested outcome.

## Performance evidence

- Hardware/toolchain: macOS 26.5.2 arm64, Apple M4 Pro, 48 GiB, 12 logical CPUs; Rust/Cargo 1.96.0 and LLVM 22.1.2.
- Workload: the 10,000 × 1,280 smoke checks source import/finalization, sequential domain/QC work, prepublished Arrow/Parquet scans and materialized reads, exact goldens, and a separate DHAT path with fresh disk-backed publication.
- Before: no C-04 embedding storage workload existed. Cold compile/link RSS is separately characterized and is not relabeled runtime memory.
- After: workspace smoke embedding interval 4.1952–4.2284 s; full 1,000,000 × 256 interval 10.446–10.488 s at 24.408–24.506 million values/s.
- Peak memory/allocations: full current-binary RSS 1,117,552,640 bytes (41.6% of 2.5 GiB); DHAT current 0, peak 324,194,945, cap 603,979,776 bytes.
- Assessment: exact correctness/resource acceptance only, not an optimization claim. Ten-million-row out-of-core behavior remains unmeasured.

## API, schema, and compatibility boundary

- IC-0011 owns the additive cell-embedding substrate. Schema/profile versions are exact; widening an enum, source grammar, physical layout, digest order, or provenance dependency requires a new reviewed version/decision.
- The root re-exports demonstrated constructors/types but adds no CLI/config/result field. Current predicted CellViT class CSV remains separate and cannot be silently treated as vectors or canonical identity.
- Physical conformance is specific to the C-04 embedding table/link profiles. It is not general Arrow/Parquet/AnnData/SpatialData/OME-NGFF/Zarr interchange and does not change current compatibility IO.
- OS mmap/zero-copy was evaluated and deferred because Marklab forbids unsafe code and has no accepted portable mapping owner. Borrowed contiguous/chunked/verified-reader parity is the supported safe path.

## Tests not run and explicit claim limits

- The checksummed public Aperio/OpenSlide oracle remains ignored because its external fixture/oracle environment is not local; all ten local WSI tests pass.
- Windows target compilation/runtime publication/recovery remains unverified because `x86_64-pc-windows-msvc` is not installed. DEC-0021 still gates Windows durable-store admission.
- Ten-million-row out-of-core embedding storage is planned only. It was not allocated or benchmarked, and no out-of-core/mmap/zero-copy claim is made.
- The authorized bundles lack the exact closed promotion fields `canonical_identity_mapping`, `input_normalization_and_run_configuration`, `reviewed_source_snapshot`, and `license_record`. They remain non-promotable.
- Patch/region/slide embeddings and `CellPatchLink`, general measurement status, embedding statistics, model execution, and patient-held-out prediction were not implemented because they belong to C-05/C-06/WS-50/WS-52.

## Decisions

- DEC-0022 through DEC-0024: layer-3 owner, explicit table/link/provenance artifacts, bounded non-pickle source and raw-before-stock physical parsing.
- DEC-0025/0026: reconciliation-only manifest identities and two-phase source adaptation/finalization.
- DEC-0027 through DEC-0029: exact Arrow/Parquet profiles, root-reviewed standalone fuzz lock, and stable checked Parquet row-group window.
- DEC-0030/0031: status-safe scans, deterministic scale workload, immutable goldens, fresh DHAT publication, and current-binary RSS ownership.

## Assumptions

1. Expected-cell, hierarchy, identity-map, coordinate-registry, spatial-context, and provenance declarations enter through a trusted producer boundary; C-04 verifies exact consistency but does not discover biological identity, calibration, or provenance.
2. The capability-confined store root is provisioned by a trusted operator under C-03's administrative assumptions; C-04 protects managed paths and cooperative operations, not hostile root administration or arbitrary trusted callback code.
3. Arrow/Parquet 56.2.1 and the root/fuzz lock tuples remain exact. A dependency upgrade or widened physical profile requires a new encoding version, allocation audit, and hostile-input regression suite.
4. The 2.5-GiB RSS threshold is host-specific runtime evidence after a current-binary prebuild; portable enforcement is supplied by explicit caller budgets rather than that one host measurement.

## Independent review and structural judgment

Two read-only reviewers iteratively audited identity/provenance/promotion semantics, hostile parsing order, retained/decoded accounting, store lifetimes, deterministic writers, differential coverage, fuzz-lock provenance, benchmark equivalence, DHAT ownership, runtime RSS, and closure evidence. Findings were reproduced with focused red tests or rejected claims before correction. Final review found no remaining blocker/high issue; reviewers edited no files and never deserialized executable model artifacts.

The large physical preflight modules remain split by format and table-versus-row-link ownership, with shared bounded primitives only where semantics are identical. The 1,132-line benchmark support file is test-only and deliberately shares one exact source/graph/physical/numeric fixture between Criterion and DHAT so their checks cannot drift; independent review found no production ownership leak. It was not split solely for line count.

## Known risks

1. Real corpus promotion remains prohibited until all four exact promotion gaps are supplied as reviewed artifacts and the graph revalidates; aggregate reconciliation is not a waiver.
2. FND-05/WS-24 remain active for C-05 patch/region/slide vectors and links plus C-06/FND-04 general measurement status.
3. DEC-0018 blocks external registry packaging until explicit versioning and dependency-ordered publication are authorized; no release action was inferred.
4. Windows durable-store runtime behavior and broad interchange remain DEC-0021/WS-25 work.
5. Ten-million-row out-of-core storage, mmap, inference, and embedding science remain unmeasured/unimplemented.

## Remaining work

1. C-05: define patch/region/slide embedding tables and `CellPatchLink` only where explicit vectors, patch identity, overlap, and observation windows exist.
2. C-06/FND-04: define general measurement status/provenance without widening C-04 extraction validity.
3. WS-25: add general interchange conformance beyond the exact C-04 Arrow/Parquet profiles.
4. Supply and review the four exact promotion fields before adapting any authorized real bundle into a canonical table.
5. Resolve DEC-0018 versioning/publication and DEC-0021 Windows runtime admission under separate explicit authorization.

## Next exact action

`rg -n "C-05|CellPatchLink|patch embedding|sampled-patch" docs/implementation crates tests`

Use the result to freeze C-05 against the authorized patch inventory without inferring vectors, sampled observation windows, or linkage that the inventory does not contain.

## Scope audit

- Existing scientific formulas, config 0.2, result 0.3, current CLI, `OutputWriter`, current CellViT class import, and compatibility CSV/Parquet behavior changed: no.
- Remote patient/source data, descendant identifiers, credentials, checkpoint bytes, `.pt`/pickle values, or absolute remote paths entered code fixtures/results: no.
- Unsafe code, mmap/zero-copy claim, whole hostile file buffering, stock decode before bounded preflight, inferred cell identity/calibration, imputation, or silent missing-row handling introduced: no.
- Registry version, permanent patch, global dependency installation, worktree, push, publish, deploy, live cloud, or remote mutation introduced: no.
- Every material API/schema/dependency/performance choice has a task contract, accepted decision, exact tests, and ledger evidence: yes.
- Worktree status: clean at Final SHA `55d1c8b`; only the reviewed C-04 closure-record diff is pending the final documentation commit while this handoff is authored.
