# Task Handoff — B-03

Status: complete
Base SHA: `f1bcc94d4a5f96825fae32676630304f31d332ea`
Final SHA: `ab3df430deb547a105608cad2f799fcf52fc9e66`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewer: `b03_policy_audit`

## Scope delivered

Parent requirement IDs: PLAT-01, WF-01, BACK-01, WS-10, B-03.

- Repository-wide Cargo gates now name `--workspace`; compatibility-only WSI, DHAT, binary, release, and public-oracle commands name `--package marklab`.
- CI compiles eight explicit feature rows and denies warnings for all features plus the affected CLI-only combination.
- Cargo-metadata architecture tests enforce `marklab` → `marklab-workflow` → `marklab-project`, reject unknown/upward local packages, recursively freeze current root CLI-gated adapter/test files, and reject CLI gating in future non-root libraries.
- Test, documentation, feature, benchmark, fuzz, calibration, package-specific, and workspace-wide policies are recorded in `WORKSPACE_POLICY.md` and `AGENTS.md`.
- No `xtask` was added because the declarative CI matrix owns the only material repetition.

## Changed files and canonical symbols

- `.github/workflows/*.yml`, `AGENTS.md`: command and feature-matrix policy.
- `tests/workflow_contract.rs`, `tests/workspace_contract.rs`: executable workflow/dependency/CLI-gating policy.
- `src/cli/batch.rs`: only the three parallel-only imports received the matching feature gate.
- `docs/implementation/**`: policy, ownership, decision, task, status, requirement, validation, and B-02/B-03 handoffs.
- Scientific algorithms, public API, config/result/schema, manifests, dependencies, and lockfiles: unchanged.

## Behavior-first and verification evidence

| Command | Result |
|---|---|
| `cargo +1.96.0 test --locked --test workflow_contract workspace_policy_is_explicit` before workflows | expected failure: missing `cargo fmt --all --check` |
| Same command after workflows | pass |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --no-default-features --features cli -- -D warnings` before import gate | expected failure: three parallel-only unused imports |
| Same command after import gate | pass |
| `cargo +1.96.0 test --locked --test workflow_contract --test workspace_contract` | 9 passed, 0 failed |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | pass |
| Eight `cargo +1.96.0 check --locked --workspace --all-targets ...` feature rows | all exit 0 |
| `cargo +1.96.0 test --locked --no-default-features --features cli --test cli --test multimodal_cli` | 35 passed, 0 failed, no warnings after fix |
| Ruby YAML parse of all five workflows | pass; not equivalent to unavailable `actionlint` |
| `cargo +1.96.0 fmt --all --check`; `git diff --check` | pass |
| `cargo +1.96.0 package --locked --workspace` at clean final SHA | pass; 247 files, 1.7 MiB/400.6 KiB compressed; verification build passed |

## Known warnings and limitations

- Compile-only no-default, CSV-only, and WSI-only all-target rows emit 14 existing test-instrumentation warnings.
- Parquet-only emits those 14 plus four unused/dead-code warnings for an internal writer whose production caller is CLI-gated.
- These rows are not claimed warning-clean. All-feature and affected CLI-only Clippy pass with `-D warnings`.
- `actionlint` remains unavailable locally; repository workflow contracts and YAML parsing passed, but no equivalent semantic lint is claimed.

## Decisions, assumptions, and remaining risk

- DEC-0012 fixes the acyclic direction: generic workflow depends on project; root owns B-04's concrete existing-engine adapter.
- The recursive CLI-gate allowlist is intentionally strict. A future legitimate adapter file must update policy and test together under a task contract.
- GitHub Actions itself was not executed locally.

## Next exact action

```bash
cargo +1.96.0 test --locked --test project_workflow
```

First add the four B-04 behavior tests and confirm the missing project/workflow API causes the expected red failure.

## Scope audit

- Unrelated, dependency, lockfile, schema, API, algorithm, or result changes: no.
- Gate weakened or removed: no.
- Duplicate implementation, ceremonial crate, or unsupported claim: no.
- Additional worktree, remote mutation, publish, or push: no.
