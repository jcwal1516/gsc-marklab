# Task Handoff — B-04 and WS-B Exit

Status: complete
Base SHA: `ab3df430deb547a105608cad2f799fcf52fc9e66`
Implementation SHA: `bcc9450ef0a2ba89f3383904a78834925f3fcf17`
Boundary-fix SHA: `ac7da28da082f795381da0a03e8437fc4a774262`
Branch/worktree: `branch/frontier-transformation` / `/Users/user/Bench/gsc-marklab`
Owner: `/root`; read-only reviewer: `b04_vertical_slice_audit`

## Scope delivered

Parent requirement IDs: PLAT-01, WF-01, WS-11, WS-12, B-04.

- Added immediately used `marklab-project` and `marklab-workflow` packages under the enforced `marklab` → workflow → project dependency direction.
- Added streaming SHA-256 content identity, reference-only input cataloging, immutable project-owned inline limits, atomically committed successful-run records, and verified reads.
- Added typed node IDs/specifications, deterministic DAG validation, complete framed cache-key material, a stateless one-node local scheduler, and explicit cache hit/miss results.
- Added `MarkedAnalysisNode`, which borrows the current `Pattern`/`AnalysisConfig`, invokes the one canonical `AnalysisEngine`, and uses the existing result-0.3 codec. No scientific formula, result projection, or filesystem writer was copied.
- Cache misses validate a decode/re-encode fixed point before commit; hits verify content identity and decode exact retained bytes. Failures before commit leave project success/cache state unchanged.
- Preserved the standalone fuzz workspace and made workspace Criterion commands select only the six explicit harness-free benchmark targets.

## Behavior-first evidence

| Behavior | Red evidence | Final evidence |
|---|---|---|
| Two real child members | Workspace contract rejected absent members | Root metadata reports exactly three packages and only root as default; fuzz metadata remains standalone |
| Digest-sensitive cache | Project/workflow API was absent | First run miss, replay hit after scheduler recreation, changed input digest miss/execution count two |
| Valid DAG | API was absent | Cycles, missing dependencies, and duplicate IDs are covered typed validation failures; direct self edges are rejected by the same graph boundary |
| Failure atomicity | API was absent; later unstable-codec test failed on missing error variant | Uncataloged/tampered inputs, execution, size, miss/hit decode, commit conflict, and noncanonical codec failures do not add success/cache state |
| Compatibility parity | Initial cache representation differed across miss/hit timing round-trip; stronger byte check then found pre-normalization bytes committed | Direct and workflow typed results match after clearing timing telemetry only; committed digest equals emitted `result.json`; `qc.json`, `report.md`, and file inventory match |
| Workspace benchmark | First WS-B exit run completed all Criterion workloads, then failed when `--quick` reached a child libtest harness | Manifest contract first failed on missing child `[lib]`; both child libs now set `bench = false`, and the exact workspace benchmark command exits 0 |

## WS-B exit gate

| Command | Result |
|---|---|
| `cargo +1.96.0 fmt --all --check` | pass |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --all-features -- -D warnings` | pass |
| `cargo +1.96.0 clippy --locked --workspace --all-targets --no-default-features --features cli -- -D warnings` | pass |
| `cargo +1.96.0 nextest run --locked --workspace --all-features` | 414 passed, 22 skipped, 1 slow; 0 failed |
| `cargo +1.96.0 test --locked --workspace --doc --all-features` | all three package doc-test binaries pass (0 tests) |
| Eight workspace all-target feature-matrix checks | all exit 0; only the previously recorded 14 narrow test warnings and four Parquet-only production warnings |
| `cargo +1.96.0 test --locked --package marklab --features wsi,cli --test wsi_integration` | 10 passed, 1 external public-slide oracle ignored |
| `cargo audit` | pass; no vulnerabilities; same two allowed unmaintained warnings |
| `cargo deny check advisories licenses bans sources` | pass; same duplicate-version warnings |
| `cargo machete` | pass; no unused dependencies |
| `cargo +nightly fuzz check` | pass |
| `env MARKLAB_BENCH_PROFILE=smoke cargo +1.96.0 bench --locked --workspace --all-features -- --quick` | pass; all six Criterion targets, eight workload intervals, no detected performance change; Plotters fallback |
| `cargo +1.96.0 test --locked --package marklab --no-default-features --features dhat-heap --lib dhat_ -- --test-threads=1` | 3 passed, 180 filtered; same 14 narrow warnings |
| `cargo +1.96.0 run --release --locked --package marklab --features wsi --bin marklab -- smoke --suite synthetic --replicates 10 --out /tmp/marklab-wsb-smoke.cbLqL7` | completed; 12/12 scenarios, 120/120 replicates, 0 failed; 14,043-byte result |
| `cargo +1.96.0 package --locked --workspace` at clean `ac7da28` | pass; 5-file project/workflow packages and 251-file root package all verified |

## Decisions, limits, and remaining risk

- DEC-0013 freezes B-04 as reference-only inputs plus bounded canonical result caching. Persistence, resume/recovery, paths/object storage, and large tables remain C-03/WS-11.
- DEC-0014 records the reviewed, already-locked SHA-256/error dependencies. Root and fuzz locks add only local package records and edges.
- `threads = "auto"` is the declared cache policy in this in-memory slice, not captured host capacity. Persistent backend/host provenance and invalidation belong to C-03/WS-13.
- The project/scheduler cap bounds retained bytes per inline artifact; materializing codecs can allocate before measurement and fixed-point validation can transiently hold two bounded buffers.
- The public Aperio/OpenSlide oracle remains deliberately scheduled because its checksummed fixture/oracle is not local. `actionlint`, Markdown lint, and Gnuplot remain unavailable locally; workflow contracts/YAML parsing and Criterion's Plotters backend are the recorded substitutes, not equivalent claims.
- The authorized remote corpus remains source material only. Stable CellViT row identity, safe `.pt` conversion, complete embedding provenance, patch links, and sampled-window geometry are unresolved and must not be inferred from filenames.

## Next exact action

Create C-01's behavior-first task contract and integration suite for typed hierarchy IDs, explicit parent links, and biological/technical replication roles. Do not couple identity to filenames or ingest the remote corpus before the stable `CellId` and schema boundary exists.

## Scope audit

- Existing scientific algorithms/config/result 0.3/`OutputWriter`/CLI/CI/fuzz targets changed: no.
- Duplicate formula, codec, writer, generic task framework, or input payload copy introduced: no.
- New registry package/version/checksum delta: no.
- Additional worktree, remote mutation, publish, deploy, or push: no.
