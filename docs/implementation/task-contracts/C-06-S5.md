# Task Contract — C-06 Declared Binary Prevalence Change

Status: complete; frozen at base `d0822bde67a93648b1a73cf61e1886c8463b6ca7`

Parent requirement IDs: `FND-04`, `CMP-01`, `WS-23`, `C-06`, `CUR-14`

## Objective and immediate caller

Add one observable runtime-only descriptive prevalence comparison over two existing `DeclaredMarkedAnalysisResult` scheduler outputs. The immediate production caller is the concrete `compare_declared_marked_prevalence` function; it reuses the exact declared-output binding and semantic-compatibility gate already required by `compare_declared_marked_prepost`.

No new scheduler node, cache codec, result/config/CLI field, physical format, receipt, validator framework, general comparison/marks/inference abstraction, randomization design, geometry owner, dependency, source adapter, or inferential statistic is authorized.

## Input and compatibility contract

- Each runtime wrapper must have result row count equal to declared row count, result mark label equal to its binary declaration, and an exact scalar-identity digest recomputed from the attached mark use.
- Additionally require `n_marked <= n_cells`, finite `p_hat`, and exact `f64::to_bits` equality with `n_marked / n_cells`; the canonical empty-row prevalence is positive `0.0`.
- Reuse the frozen C-06-S4 semantic gate and error precedence for binary mark ID/label/status, endpoint routing, optional probability ID/status, and independent/thresholded origin semantics. Exact provenance/evidence artifact IDs may differ and remain separately visible.
- The added count/prevalence checks are local shared implementation for the two immediate declared comparison callers, not a public validator or producer receipt. Arbitrary caller substitution that satisfies every available public-field check remains non-authoritative.

## Estimand, availability, and output

- Always use the binary mark counts in `MarkedPatternResult`, including when probability values were routed to structure-factor spectra.
- Report exact pre/post cell counts, marked counts, and binary prevalences. When both cell counts are positive, compute one deterministic `f64` subtraction: `post_prevalence - pre_prevalence`.
- `Available` carries `Some(delta_prevalence)`. If either side has zero cells, return `InsufficientCells` and `None` without NaN, infinity, or an inferred delta; retain each side's exact canonical prevalence, including empty-side `0.0`.
- Retain borrowed pre/post `DeclaredMarkUse`, `DeclaredScalarIdentity`, and exact timepoint references. Rows, CellIds, slides, frames, timepoints, and evidence identities may differ without implying correspondence or selecting a biological unit.
- The result is runtime-only and has no serializer. Existing `compare_declared_marked_prepost`, its inner `PrePostResult`, and all result-format 0.3 bytes remain unchanged.

Approved wording is “descriptive change in the binary marked-row proportion across two semantically matched declared outputs.” Prohibit paired-cell effects, patient/specimen prevalence, treatment effects, population inference, calibration, equivalence, noninferiority, causality, and biology.

## Behavior-first evidence

Focused cases must cover:

- genuine scheduler outputs with different rows/slides/frames/evidence and an exact hand-computed signed prevalence change;
- probability-spectrum routing still using binary counts and prevalence;
- semantic/status/threshold mismatch rejection through the shared gate;
- a legitimate empty side producing typed `InsufficientCells` and no floating delta;
- forged marked count, excessive marked count, non-finite prevalence, and finite prevalence drift rejected as invalid runtime binding;
- unchanged declared pre/post comparison and exact result-format 0.3 bytes.

Development command:

```bash
cargo +1.96.0 test --locked --test declared_marked_prepost prevalence
```

At the cohesive boundary, run the complete declared pre/post target in default and no-default modes, existing declared workflow/result compatibility, focused warning-denied Clippy/docs/format checks, one independent review, and one final workspace Nextest gate. Do not rerun C-05/C-06 fuzz, DHAT, RSS, benchmarks, packaging, or unrelated gates unless this diff invalidates them.

## Closure evidence

- Missing-surface red completed; the four prevalence-filtered cases pass, and the complete focused target passes 5/5 in default and no-default modes.
- Affected default integration targets pass 26/26 and the legacy pre/post unit filter passes 13/13.
- Focused and final workspace warning-denied Clippy, no-default workspace compilation, strict warning-denied root docs, formatting, and diff checks pass.
- The sole read-only review found eager discarded subtraction on the empty-side path. An explicit branch now performs arithmetic only for two nonempty sides; no second review was requested.
- The final `cargo +1.96.0 nextest run --locked --workspace --all-features` gate passes 881/881 in 64.648 s, with 23 skipped and one expected slow test.
- C-05/C-06 fuzz, DHAT, RSS, benchmarks, packaging, remote, and dependency gates were not rerun because this fixed-size runtime diff cannot invalidate them.

## Explicit deferrals

General mark types/formats; durable producer proof/provenance; FND-06 `InferenceDesign`; any new null/permutation/p-value; paired biological units or correspondence; patient/cohort inference; result 0.4; node/cache/CLI/file adapter; geometry/windows/weights; real-source promotion; remote/dependency/version/publish actions.
