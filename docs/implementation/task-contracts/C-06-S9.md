# Task Contract — C-06 Declared Binary Cell-Embedding Centroid Project Workflow

Status: complete; frozen at base `45a203e08ee0cd8d5e41eb06e43234117ac34c47`

Parent requirement IDs: `PLAT-01`, `WF-01`, `FND-04`, `FND-05`, `WS-11`, `WS-23`, `WS-24`, `WS-50`, `C-06`

## Objective and immediate caller

Expose the completed S7 centroid question as one observable, cacheable project execution. The concrete `DeclaredBinaryCellEmbeddingCentroidNode` consumes the existing `DeclaredScalarPatternInput`, materialized `CellEmbeddingTable`, verified `CellEmbeddingArtifact`, and exact row/component/working limits through `LocalScheduler::run_single_with_store`.

The scheduler execution is the immediate production caller of one private fixed 18-byte cache codec. No engine/CLI convenience wrapper, general result or codec framework, physical embedding format, receipt, graph, validator registry, config/result-0.3 field, dependency, source adapter, inference design, or scientific estimand change is authorized.

## Constructor, input, and cache binding

Before registering reference-only inputs:

1. Revalidate the declared input against the target project.
2. Require exact project catalog records for the verified embedding-table, row-link, and embedding-provenance ArtifactIds.
3. Run one narrow crate-private S7 binding snapshot: exact table/artifact QC, row admission, every ordered CellId, binary-group status counts, grouping digest, and component/working limit preflight, with no component arithmetic or accumulator allocation.
4. Recompute the whole compatibility Pattern content reference and declared-input reference, then register only those two ordered ordinary inputs.
5. Create dependency-free node kind `declared_binary_cell_embedding_centroid`, version 1.

The ordered semantic inputs are the existing binary provenance, optional probability provenance, optional threshold evidence, embedding-table, row-link, and embedding-provenance ArtifactIds: four to six distinct records. `run_single_with_store` must verify each exact record before both cache lookup and execution. Do not duplicate their content references as ordinary inputs or introduce a role registry.

Freeze all three scientific limits into a domain-separated configuration digest using architecture-independent big-endian `u128`/`u64` fields. Use one nonempty execution-policy literal for stored row/component order, `f32`→`f64` accumulation, mean squared component difference, and positive-zero canonicalization. The implementation identity binds crate version, node adapter v1, and private codec v1. Changing exact Pattern bytes, declared identity/evidence, embedding semantic IDs, any limit, node/spec, scheduler inline limit, policy, implementation, or codec must change the cache key.

Before every cache lookup, node input verification must recompute both ordinary references and the complete S7 binding snapshot and require exact equality with constructor state. Component arithmetic remains confined to node execution through the existing public S7 function.

## Private fixed cache codec

Output kind is `application/vnd.marklab.internal.declared-binary-cell-centroid-cache;version=1`. Exact payload length is 18 bytes:

| Bytes | Field |
|---|---|
| `0..8` | ASCII magic `MLCBCENT` |
| `8` | version `1` |
| `9` | status: `0=InsufficientGroups`, `1=Available` |
| `10..18` | big-endian `f64::to_bits()` |

Encoding first requires every output count, dimension, declaration/identity, grouping digest, embedding ArtifactId/QC/logical identity, status, and availability to match the bound snapshot. `Available` requires two nonempty present groups and one finite nonnegative value; zero must be canonical positive zero. `InsufficientGroups` requires at least one empty present group and exact all-zero filler bits. Decoding rejects wrong length/magic/version/tag, NaN, infinity, negative values, negative zero, status/count mismatch, and unavailable nonzero filler, then reattaches all counts and identities from the per-run-revalidated binding. Cached bytes never supply identity or provenance.

Encoding uses one exact fallible 18-byte reservation. Scheduler and project inline limits of 18 pass; 17 fail without a success record. The codec is an internal cache representation, not result format 0.3, a scientific receipt, or durable producer authentication. Existing public project state remains the cache trust boundary; no externally authenticated result claim is added.

## Scientific behavior and claim ceiling

Node execution delegates unchanged to `declared_binary_cell_embedding_centroid_discrepancy`. Scientific work remains conservative `present_rows * D + D`; scratch remains `2 * D * size_of::<f64>()`; the output and approved wording remain exactly S7. Constructor and per-run verification add only `O(N)` binding work and fixed metadata.

Approved workflow wording is “a cacheable project execution of the descriptive mean squared component difference between exact declared binary-group cell-embedding centroids.” It proves deterministic cache/input binding within the current project trust boundary. It does not prove spatial association, classification/separability, embedding quality, independent validation, patient/specimen effect, inference, real-source evidence, durable producer authenticity, WS-50/EMB-01 closure, or biology.

## Behavior-first evidence

- direct S7 equals scheduler miss and hit bit-for-bit and in every identity/count; exact 18-byte independent codec oracle and output kind;
- swapped binary assignments change cache key/grouping identity even when declarations, counts, and numeric value match;
- changed optional probability/evidence, embedding artifact, or any scientific limit changes the cache key without changing binary grouping semantics where appropriate;
- constructor target-project/catalog/binding failure precedes new reference registration;
- no-store execution and missing/corrupt mark, table, row-link, or provenance bytes fail before miss/hit replay and preserve success atomicity;
- insufficient groups round-trip with no numeric value;
- strict 17/19-byte, magic/version/tag, nonfinite/negative/negative-zero, unavailable-filler, and status/count codec rejection;
- exact 18-byte and one-short scheduler/project output limits;
- existing direct S7, declared marked workflow, and result-format 0.3 behavior remain unchanged.

Development command:

```bash
cargo +1.96.0 test --locked --all-features --test cellvit_embedding_artifact_graph declared_binary_centroid_workflow
```

At the cohesive boundary, run the complete affected CellViT artifact target, existing declared workflow/result compatibility, relevant package tests, no-default compilation, strict docs, formatting, warning-denied Clippy, one independent review, and one final workspace Nextest gate. Do not rerun C-05 fuzz, DHAT, RSS, benchmarks, packaging, remote, or dependency gates unless this diff invalidates them.

## Explicit deferrals

General workflow/result/codec/receipt infrastructure; CLI/engine wrapper; durable external result schema or cache import; centroid pre/post comparison; arbitrary marks; real-source promotion; geometry/windows/weights; spatial dependence; nulls/inference; patient/specimen aggregation; classification/prediction/calibration; remote/dependency/version/publish actions.
