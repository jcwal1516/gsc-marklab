# NEURAL-SET-01 — Point-set flow and diffusion

Own flow model/training and diffusion training/sampling through IC-0185. The missing-command red
preceded implementation. The likelihood is exactly invariant to point order, beats uniform Poisson,
and seeded reverse samples are supported and diverse. Claims remain iid-exchangeable synthetic.
