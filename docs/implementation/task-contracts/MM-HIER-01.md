# MM-HIER-01 — Hierarchical factor graph compilation

Own `AddHierarchicalFactorStructure` through IC-0169 and `marklab multimodal hierarchical-factor`.
The missing-command red preceded implementation. A five-node patient→specimen→region→cell hand graph
produces four conditional edges and three level-matched attachments while declaring patient as the
replication unit. This compiles a graph; it is not a fitted posterior or real-hierarchy validation.
