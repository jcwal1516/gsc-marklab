# REG-LANDMARK-UNC-01 — Landmark uncertainty and soft correspondence

Own `BayesianLandmarkRegistration`, `PropagateTransformUncertainty`,
`DeltaPropagateTransformUncertainty`, and `ProbabilisticCellCorrespondence` through IC-0182. The
missing-command red preceded implementation. GP landmark fit, MC/delta endpoint agreement, and three
soft translated matches over 0.9 pass. Output is compatibility, never asserted same-cell identity.
