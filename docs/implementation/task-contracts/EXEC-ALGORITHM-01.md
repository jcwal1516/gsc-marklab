# EXEC-ALGORITHM-01 — Unified durable typed-node invocation

Own `ExecuteAlgorithm` through IC-0198. The missing-public-symbol compile red preceded extraction.
`marklab project classical` is the immediate caller. Existing separate-process miss/hit, changed-key,
tamper, malformed-ledger, and symlink tests pass through the extracted owner.
