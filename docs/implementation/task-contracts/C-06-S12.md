# Task Contract — C-06 Declared Binary-Group Nucleus-Area Contrast

Status: complete; frozen and delivered from base `a770cdffd78e61cc185eb4a95d9142d684d1c6d5`

Parent requirement IDs: `FND-04`, `WS-23`, `C-06`

## Objective and immediate caller

Add one pathology-facing descriptive computation for the concrete question “how does declared nucleus area differ between the exact binary-marked and unmarked cells in this input?” The public `declared_binary_group_nucleus_area_contrast` function immediately consumes the existing declared scalar input, fixed `NucleusAreaUm2MarkDeclaration`, exact provenance validation, paired value digest, status, fixed result, and bounded single pass.

Add no general continuous-mark comparison, group-statistics abstraction, physical format, receipt, validator framework, workflow/node/codec, config/result/CLI field, dependency, missingness model, randomization, inference, or source adapter.

## Binding, deterministic arithmetic, and availability

Revalidate the declared input against the target project and require the exact fixed nucleus-area provenance profile. Require `Pattern::nucleus_area_um2`, exact row length, and row admission before traversal. Every area must be finite and strictly positive; every binary value is already exact `0/1` under `DeclaredScalarPatternInput`.

Frame one versioned paired-value digest from the ordered CellId digest, declared-input digest, nucleus-area provenance ArtifactId, row count, and every ordered `(binary u8, raw area f32 bits)` pair. Optional probabilities remain contextual and never enter group selection or arithmetic.

Accumulate marked and unmarked area sums in row order as `f64`. `Available` requires both groups to contain at least one row and reports each mean plus signed `marked_mean - unmarked_mean`; exact zero is canonical positive zero. Otherwise return `InsufficientGroups`, retain the nonempty group's finite mean when present, and report no difference. Runtime is `O(N)` with fixed incremental storage and no row/value allocation.

## Result identity and claim ceiling

Return fixed metadata containing status, row/marked/unmarked counts, optional marked and unmarked means in square micrometres, optional signed mean difference, the existing scalar identity, binary and optional probability declarations, the exact nucleus-area declaration, and the paired-value digest. Retain no row, CellId, binary, probability, or area array.

Approved wording is “descriptive within-input difference in mean declared nucleus area between exact binary-marked and unmarked rows.” It proves no patient/specimen effect, pairing, segmentation accuracy, classification, calibration, independence, spatial association, causal effect, inference, real-source result, MRK-01 closure, or biology. Thresholded or morphology-predicted marks may share image-derived information with nucleus area; their exact provenance remains visible and no independence claim is permitted.

## Behavior-first evidence

- exact oracle: binary `[0,0,1,1]` with areas `[10,14,20,30]` yields unmarked mean `12`, marked mean `25`, and marked-minus-unmarked difference `13`;
- repeat bits and all retained identities are exact; changing same-count binary assignments changes the paired digest and value; optional probabilities do not select groups;
- equal group means produce canonical positive zero; all-unmarked and all-marked inputs return typed `InsufficientGroups` with only the nonempty mean;
- missing/short/long/nonfinite/zero/negative/negative-zero areas plus target-project and missing/drifted nucleus provenance fail before arithmetic;
- exact row limit passes and one-short fails; existing S2/S7–S11 behavior and result format 0.3 remain unchanged.

Development command:

```bash
cargo +1.96.0 test --locked --all-features --test binary_nucleus_area_contrast
```

At the cohesive boundary, run the focused target, declared scalar/provenance and existing nucleus-area compatibility, relevant package tests, no-default compilation, strict docs, formatting, warning-denied Clippy, one independent review, and one final workspace Nextest gate. Do not rerun C-05 fuzz, DHAT, RSS, benchmarks, packaging, remote, or dependency gates unless this diff invalidates them.

## Explicit deferrals

Generic continuous/group comparison; covariate adjustment; effect size/standardization; continuous missingness; per-patient/specimen aggregation; pre/post or paired-cell contrast; nulls/permutation/p-values/intervals; spatial statistics; physical/result serialization; scheduler/CLI; real-source promotion; remote/dependency/version/publish actions.

## Completion evidence

- Behavior-first target: clean missing-surface red, then 6/6 pass; the final warning-denied target passes with one module-scoped test-only dead-code allowance for shared fixture items not used by this standalone target.
- Affected behavior: declared scalar input 9/9 and the existing nucleus-area covariance filter 7/7 pass.
- Compatibility gates: no-default workspace check, strict root all-feature docs, focused and full-workspace all-target/all-feature warning-denied Clippy, formatting, and diff checks pass.
- Sole independent review: no concrete binding, numerical, resource, API, Immediate-Caller, or claim finding; no edits, tests, or second review.
- Final workspace gate: 925/925 tests pass in 63.813 s across 65 binaries, with 23 documented skips and one expected slow synthetic test.
