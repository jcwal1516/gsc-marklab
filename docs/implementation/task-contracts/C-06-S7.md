# Task Contract — C-06 Declared Binary Cell-Embedding Centroid Discrepancy

Status: complete; frozen at base `f00a9a4f4b4ffbda97ef606195f0b291abb8d10a`

Parent requirement IDs: `FND-04`, `FND-05`, `WS-23`, `WS-24`, `WS-50`, `C-06`

## Objective and immediate caller

Add one observable scientific computation that joins the completed declared binary-mark input and verified cell-embedding flows. The concrete `declared_binary_cell_embedding_centroid_discrepancy` caller consumes one `DeclaredScalarPatternInput`, one materialized `CellEmbeddingTable`, its existing `CellEmbeddingArtifact`, and explicit row/component/working-byte limits.

No mark kind, table, physical format/profile, receipt, graph, validator framework, serializer, result/config/CLI field, workflow node, geometry/window/weights owner, null, inference design, source adapter, dependency, or real-source promotion is authorized.

## Exact binding and resource order

1. Require the supplied table's exact `EmbeddingQcSummary` to equal the verified artifact summary, thereby binding row count, dimension, every extraction-status count, zero-vector count, and logical table identity. The verified artifact already retains the embedding-table, row-link, and provenance artifact identities.
2. Enforce the caller's row-count maximum before row traversal.
3. Compare every table row's canonical `CellId` with the declared input's strictly ordered `CellId` at the same index. Group each exact embedding status by the required binary `Pattern.mark` value without component arithmetic, while producing one fixed domain-separated digest of the ordered CellId-bound binary assignments. Optional probability values never replace this binary grouping.
4. Preflight checked conservative component work `present_embedding_rows * D + D` and working storage `2 * D * size_of::<f64>()`. Exact limits pass and one-short limits fail even when a binary group is unavailable.
5. Only after all bindings and limits, allocate two `D`-component `f64` sums. Visit rows and components in canonical stored order. Non-present embedding statuses contribute no vector values.

Artifact/table and CellId failures precede component/working limits. Row-count admission precedes the O(N) identity/status pass.

## Estimand, availability, and arithmetic

For the present embedding rows in exact binary groups `M={mark=1}` and `U={mark=0}`, compute component means and:

```text
mean_squared_component_difference =
    sum_j((mean_M[j] - mean_U[j])^2) / D
```

- Promote each stored `f32` component to `f64`; sum each group in canonical row/component order; divide once per component mean; accumulate squared differences sequentially; divide once by positive `D`; canonicalize signed zero.
- `Available` carries `Some(value)` only when both groups contain at least one present embedding vector.
- Otherwise `InsufficientGroups` carries `None`; both groups' exact total/present/missing/extraction-failed/QC-rejected counts remain observable and no sums are allocated or computed after the already-enforced conservative caps.
- Promise repeat determinism only. Do not claim bitwise invariance under arbitrary row/component order or rotation.

Runtime is `O(N*D)` for available inputs plus `O(N)` binding/status work. Incremental arithmetic storage is exactly two `D`-component `f64` accumulators; retained result state is bounded metadata and fixed counts only.

## Result identity and claim ceiling

Retain availability, both group count summaries, dimension, optional value, compact declared scalar identity, exact binary declaration and optional probability declaration, the ordered binary-grouping logical digest, embedding-table/row-link/provenance artifact IDs, embedding QC summary, and logical table identity.

Approved wording is “descriptive mean squared component difference between mean present cell-embedding vectors for the exact declared binary-marked and unmarked rows.” It is not spatial association, classification accuracy, separability, independent validation, embedding quality, information preservation, a biological effect, a patient/specimen estimate, inference, calibration, real-source evidence, or WS-50/EMB-01 closure. Morphology-predicted marks may share source information with the embeddings; retained measurement/provenance identities disclose but do not resolve that circularity.

## Behavior-first evidence

- exact hand fixture with unequal canonical-profile centroids and repeat-bit oracle;
- swapped binary assignments with unchanged declarations/counts/value preserve scalar identity but change the ordered grouping digest;
- equal group centroids return canonical positive zero;
- each non-present embedding status is excluded and exactly counted; either empty present group returns typed unavailability/`None`;
- optional probabilities remain observable but binary rows alone choose groups;
- table/artifact drift and ordered CellId drift reject before zero component/working limits;
- exact and one-short row, component-work, and working-byte edges;
- exact scalar identity, binary/probability status/provenance, and embedding artifact/QC identities.

Development command:

```bash
cargo +1.96.0 test --locked --all-features --test cellvit_embedding_artifact_graph declared_binary_centroid
```

At the cohesive boundary, run the complete affected CellViT artifact target, relevant declared-input tests, embeddings package tests, no-default compilation, strict docs, formatting, warning-denied Clippy, one independent review, and one final workspace Nextest gate. Do not rerun C-05 fuzz, DHAT, RSS, benchmarks, packaging, remote, or dependency gates unless this diff invalidates them.

## Explicit deferrals

General marks or units; physical output/result schema; streaming physical computation; observation windows/geometry/weights; spatial dependence; nulls/inference; patient/specimen aggregation; technical-confounder QC; prediction/classification; cross-model comparison; real-source promotion; scheduler/CLI; remote/dependency/version/publish actions.

## Completion evidence

The missing-surface red became 4/4 focused green. The complete affected CellViT artifact graph passes 29/29, declared scalar input 9/9, and `marklab-embeddings` 56 unit + 6 domain + 1 hostile integration plus docs. No-default workspace compilation, strict root docs, focused/final warning-denied Clippy, formatting, and diff checks pass. The sole review found that declaration identity did not bind exact binary row assignments; a local domain-separated grouping digest and swapped-assignment regression close that gap. The sole post-fix workspace Nextest gate passes 890/890 in 64.580 s with 23 skipped and one expected slow test.
