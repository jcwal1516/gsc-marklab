# Task contract — GRAPH-VALIDATE-01 graph mathematics validation suite

Status: complete

Date: 2026-08-25

Owns `ValidateGraphMathematicsSuite` through IC-0157. `marklab graph validate` runs fixed independent
oracles for every live Part VII family and radius-choice sensitivity, returning one ledger row per
algorithm. kNN/kernel/barrier/component breadth, registration, sparse-memory scale, and GPU parity
remain explicit unsupported/not-applicable rows. Suite success is synthetic exact validation only.
