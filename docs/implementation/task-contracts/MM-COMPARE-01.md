# MM-COMPARE-01 — Nested multimodal M0–M5 comparison

Own `CompareMultimodalModels` through IC-0176 and canonical IC-0091. The new alias failed before
routing was added. All six models and six prescribed increments pass on 24 patient-held-out rows;
the original complementarity command also passes unchanged. Real subgroups and predictive density
remain unverified.
