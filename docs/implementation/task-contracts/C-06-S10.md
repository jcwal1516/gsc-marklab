# Task Contract — C-06 Declared Nucleus-Area Cell-Embedding Cross-Covariance Energy

Status: complete; frozen at base `45caf8991533ed16e65a6d756be40b662c49428a`

Parent requirement IDs: `FND-04`, `FND-05`, `WS-23`, `WS-24`, `WS-50`, `C-06`

## Objective and immediate caller

Add one observable descriptive computation that joins the already-loaded dense compatibility column `Pattern::nucleus_area_um2` to a verified C-04 cell-embedding table. The concrete `declared_nucleus_area_cell_embedding_cross_covariance_energy` function is the immediate production caller of every new declaration, provenance, validation, digest, status, result, and bounded arithmetic capability in this milestone.

Add no generic continuous-mark declaration, MarkTable, unit/modality registry, shared covariance/statistics kernel, physical mark format, receipt, validator framework, scheduler node/codec, config/result-0.3/CLI field, dependency, or source adapter.

## Concrete declaration and provenance

Add `NucleusAreaUm2MarkDeclaration`, fixed to:

- mark ID `nucleus_area_um2`;
- label `Nucleus area`;
- compatibility source column `Pattern::nucleus_area_um2`;
- morphology modality;
- continuous values in square micrometres;
- one non-`DerivedSummary` per-cell `MeasurementStatus`;
- one exact C-03 provenance ArtifactId.

Its exact `marklab.scalar_mark_provenance` version-one record has no table or dependencies and exactly these semantic metadata entries: `mark_id=nucleus_area_um2`, `mark_label=Nucleus area`, `measurement_status=<canonical status>`, `modality=morphology`, `unit=square_micrometer`, and `value_kind=continuous`. The computation revalidates the existing `DeclaredScalarPatternInput` against the supplied target project, then requires this exact record profile. The project catalog is provenance metadata authority only; this direct computation does not claim managed-byte verification or durable producer authentication.

Do not extend `ScalarMarkValueKind`: that enum routes the compatibility engine's binary/probability endpoints and has no immediate continuous caller.

## Binding, availability, and deterministic arithmetic

After project/declaration validation:

1. Require the nucleus-area column, exact Pattern row length, and exact table/artifact QC binding.
2. Admit table row count against `maximum_rows` before row traversal.
3. Require equal declared CellId, area, and embedding row counts; then bind every ordered CellId and require every area value to be finite and strictly positive.
4. Hash all ordered raw area `f32` bits with a versioned domain, the existing CellId and declared-input digests, the nucleus-area provenance ArtifactId, and row count. Binary assignments and optional probability values remain contextual and never enter the estimand.
5. Count present embedding rows, accumulate their area sum in stored order, and detect raw-`f32` area variation among present rows.
6. Admit conservative component work `2 * present_rows * D + 2 * D` and exact arithmetic storage `2 * D * size_of::<f64>()`, including unavailable states, before allocation or component arithmetic.

`Available` requires at least two present embedding rows and nonconstant present nucleus areas. Use two stored-order population passes: first compute present embedding-component means, then accumulate `(area_i - mean_area) * (embedding_ij - mean_embedding_j)`. Divide each component sum by the present-row count, square, sum, and divide by `D`. Accumulate as `f64`; canonicalize exact zero to positive zero.

`InsufficientPresentRows` and `NoNucleusAreaVariation` carry no energy and perform no component arithmetic or accumulator allocation after the same cap admission. The present-area mean is absent only with zero present rows and otherwise finite, positive, and positive-zero canonical by construction.

## Result identity and claim ceiling

Return one fixed metadata result containing status, present count, present mean area in square micrometres, dimension, optional energy, existing scalar identity, binary and optional probability declarations as unused context, the exact nucleus-area declaration, ordered area-value digest, embedding table/row-link/provenance ArtifactIds, embedding QC, and logical table identity. Retain no row, area, or vector array.

Approved wording is “descriptive mean squared component population cross-covariance energy between declared nucleus area in square micrometres and present verified cell embeddings.” Its numeric units are squared square-micrometre-by-embedding-component covariance units. It proves no correlation, explained variance, size normalization, segmentation accuracy, spatial association/dependence, classification, calibration, embedding quality, independence, patient/specimen effect, inference, real-source result, WS-50/EMB-01 closure, or biology.

## Behavior-first evidence

- exact 1,280-component hand oracle: areas `[10,10,20,20]` over the existing S8 vector fixture produce mean area `15` and energy `62.5`; repeat bits and identities are exact;
- changed areas `[10,10,15,20]` change digest, mean to `13.75`, and energy to `45.3125`; swapped binary assignments do not change area digest or result;
- equal vectors produce canonical positive zero; all non-present embedding statuses are excluded while QC remains retained;
- zero/one present rows and constant present areas return their exact typed unavailable states;
- missing/short/long/nonfinite/zero/negative/negative-zero area columns and missing/drifted provenance fail before arithmetic;
- target-project, table/artifact, ordered CellId, row/component/working, overflow, and allocation boundaries have explicit typed coverage; exact limits pass and one-short limits fail;
- existing S7/S8/S9, declared scalar workflow, and result-format 0.3 behavior remain unchanged.

Development command:

```bash
cargo +1.96.0 test --locked --all-features --test cellvit_embedding_artifact_graph nucleus_area_cross_covariance
```

At the cohesive boundary, run the complete affected CellViT artifact target, declared scalar/provenance and result compatibility, relevant package tests, no-default compilation, strict docs, formatting, warning-denied Clippy, one independent review, and one final workspace Nextest gate. Do not rerun C-05 fuzz, DHAT, RSS, benchmarks, packaging, remote, or dependency gates unless this diff invalidates them.

## Explicit deferrals

Generic continuous/categorical/ordinal/count/simplex/vector/posterior marks; unit conversion/registry; continuous missingness; mark Arrow/Parquet/Zarr; scheduler/codec/result serialization; geometry/windows/weights; spatial covariance/variograms; graph/kernel/local-diversity/boundary/retrieval/cross-sample methods; nulls/inference; patient/specimen aggregation; classification/prediction/calibration; real-source promotion; remote/dependency/version/publish actions.
