# MM-DROPOUT-01 — Modality-robust inference training

Own `TrainModalityRobustInference` through IC-0174. The missing-command red preceded implementation.
A pinned JAX/SciPy objective fits both anchor-preserving singleton-view patterns and each recovers
its missing held-out view below RMSE 0.3 across six patients. This is synthetic predictive
robustness, not posterior calibration or MNAR identification.
