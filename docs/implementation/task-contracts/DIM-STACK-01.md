# DIM-STACK-01 — Bayesian serial landmark stack

Own serial reconstruction, Bayesian stack fitting, and uncertainty propagation through IC-0188.
The missing-command red preceded implementation. Three shifted sections compose exactly, landmark
RMSE is below 0.05 µm, and 128 draws produce nonzero downstream centroid uncertainty.
