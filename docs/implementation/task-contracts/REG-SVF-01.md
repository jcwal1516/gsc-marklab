# REG-SVF-01 — Stationary-velocity diffeomorphism

Own `SVFDiffeomorphicRegistration` and consumed `ExponentiateVelocity` through IC-0179. The
missing-command red preceded implementation; an initial absolute-map boundary composition failed
the Jacobian oracle, and displacement composition fixed the unchanged test. Translation recovery,
positive Jacobian, and sub-0.05-pixel inverse consistency pass.
