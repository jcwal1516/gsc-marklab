# Findings Matrix

Every registered finding remains present until it is fixed, disproved with specific evidence, or explicitly deferred with a documented reason and remaining risk.

| ID | Finding | Reproduced | Resolution | Tests | Benchmark | Commit | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| COR-01 | Multimodal validation bypasses the multimodal engine | Yes — the original `synthetic_smoke/generators.rs` synthesized outcome booleans and `run_multimodal_generator` only counted them | Twenty-two multimodal negative, positive, transform, and edge scenarios construct inputs and invoke `MultimodalEngine`; marked scenarios invoke `AnalysisEngine`; pre/post controls use production comparison services. Manual flags, unconditional passing, and the many-foci tautology are removed. Reports retain failures/denominators, Wilson intervals, seed, exact scenario config, engine version, and smoke-only criteria. Scheduled calibration is separate | Production-call contract observes 24 engine calls for one full suite; required-scenario, positive, rigid, jitter, pre/post, no-injection, denominator, CLI, and scheduled calibration tests pass. Phase 9 exit Nextest 369/369 with 20 skips; standard suite 270/270 with 19 library skips | Scheduled 1,000-replicate calibration: multimodal 2.4% false positives (95% Wilson 1.62–3.55%); marked 3.3% (2.36–4.60%) | `b71b2f6`, `f47ab43`, `b660cde`, `38cf427`, `c0b6bf8`, `20803d3` | Fixed |
| COR-02 | Configured rigid registration is not rigid | Yes — `Rigid` dispatched to `fit_similarity`, which returned diagonal scale plus translation and omitted rotation | Replaced with normalized closed-form orientation-preserving 2-D rigid least squares; removed unused scale-plus-translation fit; library and CLI dispatch now agree and metadata reports `rigid` | Identity, translation, 90-degree/arbitrary rotation, rotation+translation, distance, scaling, degeneracy, noise, finite coefficients, reflection, public-engine, and CLI-sidecar tests | N/A | `53e2348` | Fixed |
| COR-03 | Stratified confounding comparison recomputes the same result | Yes — configured strata produced the primary spectrum, then `stratified_confounds` reran the same stratified spectrum | Distinct stratified primary and unstratified sensitivity share modes/observed powers. Format 0.3 persists both inference summaries, primary-null identity, alpha, typed availability, and typed conclusion; homogeneous strata retain the unstratified result and mark the stratified result degenerate | Four conclusion-rule tests; `distinct_nulls_are_actually_executed` execution/value/round-trip assertions; homogeneous/missing-strata tests; report projection; full Nextest 329/329 | Reuse is structural; Phase 7 spectrum benchmark pending | `aecc554`, `2a7fa2b` | Fixed |
| COR-04 | Non-finite enrichment results can break serialization | Yes — zero expected edges with positive observed edges returned `f64::INFINITY`; zero null variance was forced to z-score zero | Result format 0.3 makes ratio/z-score nullable and adds typed reasons for zero expectation, zero variance, insufficient null samples, and defensive non-finite computation; p-values remain available; JSON, CSV, Parquet, and reports are updated | Enabled sparse finite-state and JSON round-trip tests; zero-variance test; nullable Parquet output test; full Nextest 283/283 | N/A | `4bf20e8` | Fixed |
| COR-05 | Unavailable or invalid states are represented by numeric zero | Yes — profile/pre-post errors used `statistic: 0.0`; empty pair bins used `value: 0.0` | Empty pair bins are `None` and inference-ineligible; unavailable comparisons have typed availability, a null statistic, and an explicit reason | Enabled empty-bin regression; pair-envelope, pre/post diagnostics, profile comparison, JSON, CSV, and Parquet coverage | N/A | `e7447c0` | Fixed |
| COR-06 | Exact floating-point axis equality is used in pre/post comparison | Yes — spectrum, pair-correlation, and cross-curve axes used direct `f64 != f64` checks | All three axes use one documented absolute-plus-relative tolerance and retain typed diagnostics for non-finite, length, availability, or material mismatches | Enabled harmless-reconstruction test covers all curve families; material spectrum mismatch and existing length/bin mismatch tests | N/A | `e7f91ca` | Fixed |
| COR-07 | Internal-control validity semantics may be conflated with overall retained fraction | Yes — both CSV and Parquet assigned `internal_control_valid_fraction = valid_mask_fraction`, whose numerator was final retained rows | One shared counter model tracks in-mask, tumor-valid, IHC-valid, control-valid, artifact, nonviable, and retained counts; all fractions use the in-mask denominator; zero denominators error | Enabled reproduction; combined exclusions; valid-tumor/IHC failures; CSV/Parquet parity; zero-denominator; result propagation | N/A | `6000cc8` | Fixed |
| SCI-01 | The MODWT implementation is not an MODWT | Yes — no wavelet/scaling filters or transform coefficients exist; code combines neighbor-difference energy and 2x2 block-mean variance | Format 0.3, config, modules, artifacts, reports, examples, and benchmarks use multiscale residual/scale-energy terms; old config and result aliases are rejected | Config/result negative alias tests; relative-energy checkerboard/gradient tests; engine/output/CLI suites; full Nextest 292/292 | Manual three-size territory/profile smoke benchmark completed; release baseline retained for later comparison | `ada6159` | Fixed |
| SCI-02 | The DoG module does not implement a difference of Gaussians | Yes — module only returned `scale_um * sqrt(2)` | Replaced DoG module/name with literal `scale_radius::neighborhood_radius_from_scale`; docs state no Gaussian filtering occurs | Scale-to-radius conversion test and obsolete-term source search | N/A | `ada6159` | Fixed |
| SCI-03 | Wavelet territories are neighborhood residual heuristics | Yes — circular neighborhood marked fraction, binomial standardized residual, greedy overlap suppression; no transform response | Renamed residual-neighborhood territories; introduced distinct `ResidualTerritory` fields; QC overlap is nullable rather than constant zero; GeoJSON/artifacts updated | Residual maxima, typed fields/QC absence, GeoJSON schema, output artifact, pre/post, and engine tests | Manual marked-territory 256/512/1024 smoke run completed; Phase 6 optimization pending | `ada6159` | Fixed |
| SCI-04 | Audit the “Bartlett periodogram” name | Yes — one Hann-tapered raster FFT was used; no segment averaging; `low_k_shells` selected sorted individual modes rather than shells | Renamed to Hann-tapered raster periodogram; radial annuli use the longer raster dimension's minimum Fourier spacing, average all modes per shell, and give nonempty shell means equal weight | Independent known-raster shell oracle, finite diagnostic, engine-spectrum integration, full Nextest 293/293 | Criterion `marked_analysis_periodogram_grid64`: 12.625–12.790 ms, median estimate 12.703 ms; release-baseline comparison remains Phase 12 | `35857b4` | Fixed |
| SCI-05 | Generic marked-pattern analysis emits MMR-specific interpretation text | Yes — generic `Pattern` engine hardcoded MMR-IHC and clonality prose in `interpretation_for` and report | Core classes/text and marked report are neutral to the configured mark; MMR language remains only in explicitly multimodal/MMR-specific surfaces | Generic result/report, suppression wording, pre/post language, multimodal report, CLI, and full suite | N/A | `ada6159` | Fixed |
| SCI-06 | Pair correlation name is used for centered mark-pair covariance | Yes — bin statistic is mean `(m_i-p)(m_j-p)`, not density-normalized point-process `g(r)` | Module, functions, results, config, stage timing, artifacts, pre/post comparisons, tests, and docs now use mark-pair covariance; point DTO fields are `covariance` and `pair_count`; cross-interaction points are a distinct type; obsolete result/config names have no aliases | Independent centered-product fixture, empty-bin typed-state regression, result/config negative alias tests, engine/prepost/output/CLI coverage, full Nextest 295/295 | Release medians 37.250 / 134.917 / 415.041 µs at 256/512/1,024; ratios 3.62×/3.08× confirm PERF-04 remains open | `2449e3f` | Fixed |
| SCI-07 | Curve equivalence test is only a margin assessment | Yes — it compared one descriptive distance with a margin and calculated no inferential equivalence p-value | Renamed module/function/config/scenario/report surfaces to descriptive margin terms; format 0.3 uses `margin`/`within_margin` and removes `p_equivalence`; comparison algorithms are no longer CLI-gated; zero is a valid exact-match margin | Red/green schema test, zero/negative/non-finite boundaries, absent-margin and insufficient-data states, profile/prepost/report/config negative alias tests, full Nextest 296/296 | N/A | `2191974` | Fixed |
| SCI-08 | Beta-binomial diagnostic is fixed-prior beta posterior summaries | Yes — independent Beta(1,1)-Binomial conjugate summaries; no beta-binomial marginal/dispersion fit | Module/function/config/result/types/timing/report/CLI/examples use beta-posterior-group terms; diagnostic identifier is `beta_posterior_group_summary_v1`; docs state fixed prior, independent groups, no shared dispersion model, and exploratory scope; obsolete config/result names have no aliases | Red/green schema test, posterior/group fixtures, even-median grouping, config/result negative aliases, CLI/report/multimodal rejection tests, full Nextest 298/298 | N/A | `1e8fbbd` | Fixed |
| SCI-09 | Curve difference “test” overstates pooled-bin shuffling | Yes — already-aggregated bins are pooled and shuffled under an approximate diagnostic without spatial exchangeability | Module/function/seed/result fields and collections use pooled-bin diagnostic/comparison terms; comparison DTO has typed method and `pooled_bin_p_value`; reports/docs retain non-spatial limitation | Red/green schema test; deterministic seed, nonzero statistic, zero-permutation, pre/post axis/availability, report, JSON, multimodal CLI, full Nextest 299/299 | N/A | `69005bb` | Fixed |
| ARCH-01 | Library and CLI perform different multimodal analyses | Yes — the engine returned only its primary result while the CLI refit registration, rebuilt the graph, and calculated configured null-model sensitivities, residuals, and extrapolation as CLI-only sidecars | `MultimodalAnalysisRun` owns the one fitted transform, one graph, primary result, every configured null sensitivity, registration residuals, and typed extrapolation assessment; CLI projections consume that run and direct library/CLI core results match | One-fit/one-build, all-null presence, library/CLI parity, residual/hull boundary/degenerate/empty/order tests, and sidecar integration | Final complete-run benchmark records the corrected larger application workload at 0.520/1.278/3.445 ms and +7.5% peak RSS; comparison is non-equivalent because the baseline omitted configured sensitivity/artifact work | `698226c`, `b97dfb3` | Fixed |
| ARCH-02 | AnalysisEngine is a distributed god workflow | Yes — `analyze_pattern_inner` coordinated validation, component planning, spectral planning/execution, sensitivity, interpretation, covariance, territories, anisotropy, multiscale energy, diagnostics, timing, and assembly across parent scope | `MarkedAnalysisRun` owns the application result/context. The coordinator is now 99 lines and delegates explicit validation/component planning, typed spectrum planning/execution, typed pooled spatial diagnostics, sensitivity status policy, diagnostics, interpretation, timing annotation, and result assembly. Touched modules use explicit imports | Public marked-run contract, 20-test engine-spectrum differential suite, full Nextest 313/313, CLI workflows | Complete marked baseline recorded; stage benchmarks Phase 7/12 | `82751fe`, `e29bd2c` | Fixed |
| ARCH-03 | structure_factor.rs is a god file | Yes — the 1,207-line file mixed Fourier kernels, mode/band planning, permutation workers, shell aggregation, scalar summaries, result assembly, and tests | Preserved the public facade and stable function paths while extracting explicit `kernel`, `modes`, `shells`, `permutation`, and `summaries` modules. Phase 7 added checked contiguous matrices, one fixed shell plan, one compact mark-field executor, bounded directional anisotropy, matrix-native ERL, and explicit application context without parent wildcard coupling. | Core numerical/property suite; exact binary/continuous/stratified chunk equality; anisotropy dense differential; 358-test Nextest; all-feature Cargo suite; denied-warning Clippy; no-default check | Numerical behavior preserved; Phase 7 timing/RSS comparison recorded, final system comparison remains Phase 12 | `38700da`, `89c7421`, `3b790b2`, `8681338`, `efb8187`, `85ed0d1`, `efa1089` | Fixed |
| ARCH-04 | CLI multimodal analysis contains domain logic | Yes — `cli/multimodal/analyze.rs` fitted transforms, constructed graphs, derived null strata, ran enrichment permutations, calculated hull/extrapolation/residuals, and projected scientific CSVs | All scientific computation is owned by `MultimodalAnalysisRun`; a focused output adapter consumes the run and writes every result/sidecar projection. The 106-line CLI module now parses/loads/invokes only; landmark decoding remains input-adapter cleanup under BOUND-02 | Call-count, configured-null, library/CLI parity, hull/residual edge cases, 21-test multimodal CLI suite, output suite | Final complete-run benchmark and transactional-output benchmark measure application analysis and adapter writing separately; output medians are 0.874/0.859/0.939 ms with 12.27 MiB peak RSS | `698226c`, `b97dfb3`, `a29885b`, `99f6d78` | Fixed |
| ARCH-05 | Configuration is a god file | Yes — the 565-line module combined the public configuration model, hand-written serde, section/default construction, path-aware TOML decoding/override merging, and a 134-line cross-field validator | Preserve `config.rs` as a 12-line private facade and assign the unchanged model, defaults, TOML decoding/merging, and validation contracts to explicit modules of 225/108/65/179 lines. Public crate-root types and methods are unchanged; no migration layer was needed because serialized behavior did not change | 8/8 configuration contracts; no-default check; denied-warning Clippy; full Nextest 372/372 | N/A | `5de8304` | Fixed |
| ARCH-06 | Pre/post comparison is a god file | Yes — one 631-line file mixed marked and multimodal orchestration, scalar deltas, curve diagnostics, three axis families, territory statistics/matching, and interpretation policy | Replaced with distinct marked and multimodal services plus single-owned context policy, tolerant axes, generic curve diagnostics, and generic two-implementation territory summaries. No parent wildcard imports; largest production module is 163 lines | 11 unit tests including direct multimodal service, harmless/material axis cases, unavailable curves, territory matching; both CLI flows; full Nextest 316/316 | N/A | `3ed6914` | Fixed |
| ARCH-07 | Output writer is a god file | Yes — the 525-line writer owned result-document construction/parsing/finite validation, marked and multimodal projections, transaction orchestration, manifest validation/rebasing, and shared JSON/timing writes despite existing family and transaction modules | Result-document semantics live in `document`; marked and multimodal result projections live with their analysis families; validated JSON/timing writes have one 38-line shared adapter. The 230-line writer now owns only artifact planning, same-filesystem transaction/commit, manifest status/validation, and path rebasing. Application-run CSV sidecars remain separately CSV-gated | 15 output transaction/manifest tests; 8 result-schema tests; 21 multimodal CLI tests; no-default check; denied-warning Clippy; full Nextest 372/372 | Fifteen-sample full transactional-output medians are 0.874/0.859/0.939 ms at 64/128/256 cells with 12.27 MiB peak RSS; analysis is excluded | `a0dee6d`, `99f6d78` | Fixed |
| ARCH-08 | Result types are a schema landfill | Yes — `MarkedPatternResult` carried registration, fused-cell, neighborhood, cross-curve, profile, and territory-comparison placeholders even after a distinct `MultimodalResult` existed; all DTOs occupied one result-types file | Format 0.3 marked/multimodal payloads are disjoint. DTOs now have six cohesive owners—common, marked, multimodal, pre/post, diagnostics, and artifacts—behind a 32-line facade; nested DTOs reject unknown fields. Unsupported comparison orchestration and application metadata are no longer crate-root API | Marked-payload absence; distinct pre/post; 0.3/nested unknown-field/unknown-version round trips; API contract; Phase 10 Nextest 372/372 | N/A | `51564c8`, `adda397` | Fixed |
| ARCH-09 | Cosmetic modularity | Yes — a 570-line catch-all marked stage, 1,232-line mixed validation workflow, 563-line mixed generator, 416-line four-schema Parquet writer, 227-line result assembler, two production parent wildcards, and four 1–7-line compatibility/helper files remained after boundary fixes | Delete the catch-all stage; separate marked/multiscale spatial stages, validation model/statistics/policy/runners/observation/generators, Parquet schema families, spectrum/result assembly, and interpretation policy. Remove production wildcards. Merge residual/radius/taper helpers into their only cohesive consumers and delete the RNG compatibility re-export. `CODE_SHAPE_AUDIT.md` records every retained trigger and dependency rationale | Geometry reuse; 20 engine-spectrum; complete production validation; 7 Parquet parity/export plus multimodal artifact; 16 algorithm; 16 inference; 28 neighborhood; no-default; Clippy; Phase 11 Nextest 372/372 and full Cargo suite | Existing Phase 6/7 benchmarks guard moved numerical paths; no numerical behavior changed | `1f64d03`, `b724013`, `87a5e4f`, `4aaed57`, `2ded57a`, `e5cf7e0` | Fixed |
| BOUND-01 | Pattern opens files | Yes — `Pattern::from_paths` read and parsed mask files and dispatched cell-table I/O from the domain type | Removed the filesystem constructor. Public `PatternLoader` binds a parsed `TumorMask`, dispatches CSV/Parquet adapters, and optionally returns honest decode/filter and indexed-nearest diagnostics; `Pattern` contains only validated data/construction | Public loader API contract; incremental CSV visitor; CSV/Parquet parity; affected CLI loader tests; full Nextest 344/344 | 10k and 1m loader benchmarks exercise `PatternLoader` | `eb4f5b0` | Fixed |
| BOUND-02 | Cell DTOs, CSV loading, CellViT adaptation, validation, and label interpretation share one module | Yes — one 328-line `cell_table.rs` owned domain cells, generic H&E/IHC CSV I/O, CellViT normalization, row validation, and allocating label policy | Replaced with cohesive domain cells, borrowing label policy, shared row validation, generic CSV adapter, and CellViT adapter modules; root public cell types are preserved and CLI invokes the adapters | 20 multimodal unit tests, row/path diagnostics, CellViT CLI, 57 neighborhood tests, full Nextest 315/315 | N/A | `dc9ffeb` | Fixed |
| BOUND-03 | Domain enrichment is compiled only under the CLI feature | Yes — stratified enrichment and its deterministic seed namespace were guarded by `feature = "cli"` solely because the CLI owned null sensitivity | Removed the CLI gates and invoked stratified enrichment from the public multimodal application run; analytical behavior is now identical in no-default library builds | Configured-null application test; no-default-feature check; full Nextest | N/A | `698226c` | Fixed |
| BOUND-04 | Output code calculates domain policy | Yes — domain residual, extrapolation, strata, and null-model calculations were interleaved with CLI sidecar writing | Calculations moved to the application/domain run; the output adapter receives formed results/artifacts and only validates/serializes JSON and CSV projections. Output-module audit found no transform fit, graph build, enrichment, hull, or statistical execution | Multimodal sidecar equivalence, finite boundary, report/output, and full CLI coverage | N/A | `698226c`, `b97dfb3`, `a29885b` | Fixed |
| BOUND-05 | WSI adapter is comparatively clean | Pending | Pending investigation | Pending | Pending | — | Open |
| DUP-01 | Multiple median implementations | Yes — five production median implementations used two incompatible even-sample rules | Replaced by canonical average-even median functions with explicit reject/ignore non-finite policies; beta-binomial coordinate grouping now uses the corrected average-even rule | Common statistics unit tests, beta-binomial even-group regression, full Nextest | N/A | `8508671` | Fixed |
| DUP-02 | Multiple mean, finite-mean, variance, min/max, and effective-length helpers | Yes — production contained duplicate means, finite means, variances, standard deviations, and finite extrema | Consolidated semantically identical statistics into named functions; deliberately distinct reject/ignore and population/sample contracts remain separate | Common statistics unit tests plus existing caller suites | N/A | `8508671` | Fixed |
| DUP-03 | Permutation p-value formula is duplicated | Yes — enrichment, cross-interaction, and curve-difference paths duplicated inclusive high-tail plus-one calculations | All scalar paths now use one implementation with explicit tail, inclusive ties, plus-one correction, finite policy, and minimum permutation count | `permutation_high_tail_inclusive_ties`, `permutation_low_tail_inclusive_ties`, `permutation_two_sided_equal_tail`, `permutation_rejects_nonfinite`, explicit-minimum test, full Nextest | N/A | `8508671` | Fixed |
| DUP-04 | Stratified and unstratified enrichment duplicate the full algorithm | Yes — both public functions repeated validation, label materialization, observed counting, pair iteration, result construction, and multiple-testing adjustment, differing only in label permutation strategy | One execution core now owns validation, observed counts, permutation evaluation, result construction, and BH adjustment; a small enum injects only source-section versus explicit-strata shuffling and preserves distinct seed domains | Pinned deterministic outputs for both strategies, mismatch validation, 16-test enrichment suite, configured-null application and sidecar tests | Numerical preservation fixture; performance benchmark remains Phase 12 | `b233104` | Fixed |
| DUP-05 | CSV and Parquet loading duplicate the same state machine | Yes — both loaders separately allocated every retained column/encoder/counter, applied the same mask/QC/exclusion rules, enforced metadata and dense-option invariants, constructed `Pattern`, derived window geometry, and applied fractions | `DecodedCellRow` is one logical schema; CSV and Arrow adapters map physical inputs into it; one `PatternBuilder` owns validation, mask/QC filtering, metadata, dense optional columns, categorical encoding, geometry, counters, and finalization | `csv_parquet_equivalent_rows_produce_equal_pattern`; nine CSV loader contracts; seven Parquet I/O contracts; full Nextest 317/317 | Existing CSV/Parquet baselines retained; final performance comparison Phase 12 | `f4243cd` | Fixed |
| DUP-06 | Registration and graph construction are repeated by CLI and engine | Yes — the engine fit/built both, then CLI multimodal analysis fit/built both again for sidecars and null sensitivity | Application run retains the canonical fitted transform and graph; CLI consumes them and contains no transform-fit or graph-build call | Exact one-fit/one-build tests; library/CLI parity; multimodal sidecar integration | One invocation asserted; full timing comparison pending | `698226c` | Fixed |
| DUP-07 | Timing and manifest construction are duplicated | Yes — marked output cloned/mutated analysis stages, CLI reread `timings.json` to produce timing/trace projections, and marked CLI, generic marked, and multimodal writers independently built incompatible manifests | Timing/result/trace projections use one in-memory `Vec<TimingStage>` history. One typed `RunManifest::from_document` builder owns common marked/multimodal result/output/timing fields and accepts optional typed CLI input/execution context; all prior JSON constructors are deleted | Enabled telemetry equality; marked and multimodal manifest assertions; external timing equality/trace; eight analyze CLI tests; full Nextest 322/322 | Output-write time is intentionally outside analysis telemetry and remains a separate Phase 12 benchmark | `756ecbc`, `3d8ad46`, `5d5f8d2` | Fixed |
| DUP-08 | Curve-test DTO construction is duplicated | Pending | Pending investigation | Pending | Pending | — | Open |
| DUP-09 | Effective geometry is calculated using inconsistent definitions | Pending | Pending investigation | Pending | Pending | — | Open |
| PERF-01 | spatial_index.rs is quadratic and is not a spatial index | Yes — source was a nested all-pairs loop; doubling took 3.45× and 3.30× | `SpatialIndex2D` bulk-loads immutable finite coordinates into `rstar` 0.13.0 and provides exact nearest, kNN, indexed-radius, external-point radius, and allocation-free visitor queries. Multimodal graph/DBSCAN/profiles share one run index; marked pair/residual plans share one spatial-stage index. Base memory is subtracted from the configured budget; conservative index plus peak non-overlapping plan storage must fit, stops before an over-budget entry, and is included in telemetry | Query brute-force cases; multimodal one-index; pair/residual one-plan and separate budget rejection; public 1 MiB rejection/8 MiB telemetry; Phase 6 Nextest 349/349 | Nearest, graph, plan, 1,024–16,384 scaling, RSS, and no-regression post-guard checkpoints recorded | `3c4a255`, `10e4932`, `968d014`, `9d01b04` | Fixed |
| PERF-02 | Radius graph construction is quadratic | Yes — inspected all-pairs graph builder; doubling took 3.53× and 3.51× at fixed density | Graph radius edges use shared-index inclusive queries, retain only target > source, store each undirected edge once, and reuse the returned distance for result construction | `graph_matches_bruteforce`; inclusive radius, duplicates, invalid coordinates/error, combined-union, registration-resolution tests; full Nextest 337/337 | 256/512/1,024 medians 0.109/0.311/0.565 ms with identical 930/1,913/3,906 edges; ratios 2.85×/1.82× | `3c4a255` | Fixed |
| PERF-03 | kNN graph construction sorts all other cells for every cell | Yes — source sorted every other cell per node; doubling took 4.02× and 4.28× | kNN uses indexed nearest iteration, includes all equal-distance candidates at the cutoff, sorts by distance/index, normalizes undirected pairs, and reuses stored distances | `knn_matches_bruteforce`, duplicate/tie/order tests, `graph_matches_bruteforce`, oversized-k and combined graph, full Nextest 337/337 | 256/512/1,024 medians 0.172/0.635/0.841 ms versus 0.957/3.850/16.462 baseline; ratios 3.70×/1.32× | `3c4a255` | Fixed |
| PERF-04 | Pair correlation recalculates all pair distances | Yes — nested pair loop rebuilt geometry for the observed labels and every permutation | Accurate `MarkPairCovariancePlan` stores source/target/bin assignments once in original pair order, represents empty bins explicitly, and evaluates observed/permuted marks without distance work. The API stage builds exactly one plan for all curves | `pair_plan_matches_bruteforce`; `pair_geometry_is_reused_for_observed_and_permutations`; empty-bin and full engine-spectrum coverage; full Nextest 341/341 | Plan evaluation 0.031/0.056/0.109 ms and 19 evaluations 0.560/1.038/1.815 ms at 256/512/1,024; build crossover documented | `10e4932` | Fixed |
| PERF-05 | Territory detection repeats neighborhood scans | Yes — marked candidates scanned every cell at every scale/permutation and multimodal DBSCAN neighbor lists were all-pairs | Multimodal DBSCAN queries the one application index and preserves prior neighbor order. Marked `ResidualTerritoryPlan` stores eligible per-scale memberships in contiguous offsets/indices, preserves cell-index summation order, and serves observed plus all null labels; raster nulls no longer clone `Pattern` | Exact `territory_neighbors_match_bruteforce`; exact `residual_territory_plan_matches_bruteforce`; one-plan observed/permutation regression; affected engine tests; full Nextest 343/343 | Observed+19 residual totals 0.222/1.086/2.013 ms versus estimated old 0.620/3.180/12.500; multimodal 1,024–8,192 ratios all below 4× | `10e4932`, `968d014` | Fixed |
| PERF-06 | Profile membership repeatedly scans every cell | Yes — `profile_for_territory` iterated all fused cells for every territory | Profiles use allocation-free external-point radius visits against the same `MultimodalEngine` index used by graph and DBSCAN; complete profile output matches a brute-force oracle | `territory_profile_membership_matches_bruteforce`; boundary/label/registration-resolution/error suite; application one-index assertion; full Nextest 343/343 | 1,024/2,048/4,096/8,192 points with 128/256/512/1,024 territories: 0.192/0.573/0.781/2.530 ms; ratios 2.98×/1.36×/3.24× | `3c4a255`, `10e4932`, `968d014` | Fixed |
| PERF-07 | Million-cell benchmark is not credible with quadratic nearest-neighbor calculation | Yes — benchmark built one giant CSV `String`, CSV decoding collected every owned row before building, and finalization invoked the former quadratic nearest-neighbor scan | Fixture rows stream through `BufWriter`; CSV rows visit the shared builder incrementally; indexed nearest-neighbor finalization is retained. Criterion excludes fixture generation and reports full load, decode/filter, and nearest stages separately; full remains scheduled/manual | Incremental visitor stops before malformed later row; workflow source contract; public loader; CSV/Parquet parity; benchmark build/smoke; full Nextest 344/344 | Manual 1,000,000 rows: 2.548–2.571 s full, 266.35–267.88 ms decode/filter, 2.268–2.275 s nearest; 448,856,064-byte peak RSS | `eb4f5b0` | Fixed |
| PERF-08 | Spectrum stores large nested mode-power matrices | Yes — binary, continuous, and stratified spectrum paths allocated `B × modes` nested vectors and `k_chunk_modes` had no execution effect; anisotropy separately retained a directional mode matrix | Primary permutations now accumulate configured mode chunks into checked contiguous `B × nonempty-shells` rows consumed directly by ERL/leave-one-out inference. One compact binary/continuous/stratified mark-field executor reuses scratch. Directional anisotropy retains only `B × min(k_chunk_modes, directional modes)`, accumulates tensors, and discards each chunk. The application memory estimate accounts for shell summaries, anisotropy chunks, and worker scratch. | Exact chunk equality at 1/typical/oversize values; storage-shape and zero-chunk contracts; anisotropy dense differential for both nulls; reusable fixed/stratified scratch; ERL oracle; one/multiple-thread equality; 358-test Nextest and full Cargo suite | At 999 permutations, theoretical primary matrix falls 6,361,632→63,936 bytes and measured RSS 16,515,072→8,896,512 bytes (−46.1%). Default 256-mode timing is −25%/−6%/+2% binary and −13%/+1%/+5% continuous at 64/128/256 cells; tight 64-mode chunks carry a documented 20–41% cost. Anisotropy is 4.8–5.3× faster. | `efb8187`, `67f83f7`, `85ed0d1`, `43e5d74`, `93cdda9`, `c183942`, `efa1089` | Fixed |
| PERF-09 | Repeated metadata and label allocations | Yes — every `FusedCell` cloned case/timepoint/protein and every `primary_label` call allocated a new String; graph/enrichment/curve/profile loops retained those allocations | `FusedCell` contains only per-cell data; `AnalysisMetadata` is created once and moved into the result; CSV/Parquet join shared metadata during writing. Label access returns borrowed/static `&str`; enrichment, cross curves, profiles, territories, and graph smoothing use borrowed labels/compact indices | Pointer-identity allocation-free label regression, domain serialization absence, CSV flattened metadata values, Parquet artifacts, neighborhood/diagnostic suites, full Nextest | Final complete multimodal RSS is 8.73 MiB (+7.5%) while performing all configured analyses; transactional output is 12.27 MiB. No per-cell metadata clone remains, and the million-row path is +0.5% RSS versus Phase 6 | `dc9ffeb` | Fixed |
| PERF-10 | Complete results and cell tables are cloned for output | Yes — CLI cloned marked/multimodal results and previously cloned fused cells before output | Both marked and multimodal output paths now consume application runs, move results into versioned documents, and borrow projections; production source contains no complete-result or fused-table output clone | Public marked-run contract, 15-test marked CLI suite, 21-test multimodal CLI suite, output suites, production source check | Dedicated full transactional-output benchmark consumes a precomputed run and records 0.874/0.859/0.939 ms medians plus 12.27 MiB peak RSS; complete-run RSS is +4.7% marked and +7.5% multimodal | `a29885b`, `82751fe`, `99f6d78` | Fixed |
| MODEL-01 | TerritoryFeature overloads unrelated algorithms | Yes — residual-neighborhood and multimodal DBSCAN results shared a generic DTO with derived `scale_um`, duplicate `z_or_power`, optional pseudo-component identity, and unimplemented QC overlap | Marked residuals use `ResidualTerritory`; multimodal DBSCAN clusters use `NeighborhoodTerritory` with explicit abnormal-cell support and required cluster ID. Ambiguous and unimplemented fields are removed without aliases | Multimodal CLI result-shape regression; DBSCAN/profile/pre-post/GeoJSON tests; full Nextest 329/329 | Existing territory/profile baseline compiles with the new DTO; indexed performance remains Phase 6 | `51564c8` | Fixed |
| MODEL-02 | Public fields are present but not implemented | Yes — `p_equivalence`, empty territory-profile enrichment/cross-curve vectors, constant/nullable-but-never-produced QC overlap, empty multimodal timings, and compatibility aliases were confirmed | Removed equivalence and compatibility placeholders, both empty profile vectors, and both never-produced QC-overlap fields. Multimodal results now contain ordered application telemetry for validation, fit/QC, fusion, index/graph, artifact projections, primary and per-null enrichment, curves, territories/profiles/comparison, diagnostics, and assembly. Result and sidecar derive from the same vector. | 0.3 schema/migration assertions; profile/GeoJSON/marked-schema; exact multimodal stage-order/dimension/finite telemetry; CLI result-sidecar equality; library/CLI core parity; Nextest 359/359 | Final complete multimodal benchmark records the full stage-owned workload and +7.5% peak RSS; result/timing equality remains test-enforced | `2191974`, `b71b2f6`, `51564c8`, `8ba7c97` | Fixed |
| MODEL-03 | String statuses and interpretation classes | Yes — result documents exposed free-string overall status, interpretation class, registration transform kind, primary endpoint kind/null, and multiscale energy band; registration also carried a redundant constant `"ok"` status | Closed serde enums now own every machine-facing category: `AnalysisStatus`, `InterpretationClass`, `TransformKind`, `PrimaryEndpointKind`, `SpectrumNullModel`, and `ScaleEnergyBand`. Unknown values and nested fields are rejected; display/report prose is separate; redundant registration success status is removed | Unknown status/class/transform/endpoint/null/band and nested-field rejection; registration-shape and 0.3 round trips; engine/output/prepost/registration suites; Phase 10 Nextest 372/372 | N/A | `f7a930f`, `adda397` | Fixed |
| MODEL-04 | Component modes are not behaviorally distinct | Yes — `Separate` and `Both` shared one branch and pooled analysis always ran | Explicit plan resolves Pooled, Separate, Both, or Auto; Separate skips pooled endpoint work and emits NotApplicable; result records request, resolution, and reason; Auto threshold is largest fraction < 0.80 with multiple components | Enabled Separate regression; Pooled-only, Both, Auto-pooled, Auto-both, selection-reason, schema round-trip, and full engine-spectrum coverage | Separate avoids pooled spectrum/curve/anisotropy/multiscale work; formal benchmark deferred | `b56cc60` | Fixed |
| OUT-01 | Result and timings artifacts describe different timing histories | Yes — marked writer cloned result timings and appended `write_outputs` only to the sidecar | Result and timing sidecars serialize the exact same analysis-stage vector; `write_outputs` is removed because output cost is not scientific analysis telemetry. External timing/trace files derive in memory from that same vector | Enabled `result_and_timings_use_same_telemetry`; output suite; external timing equality and trace tests; full Nextest 322/322 | Output writing is now a separate measured workload: 0.874/0.859/0.939 ms at 64/128/256 cells, deliberately outside analysis telemetry | `756ecbc`, `3d8ad46`, `99f6d78` | Fixed |
| OUT-02 | Pre/post results are unversioned | Yes — both CLIs wrote a bare `PrePostResult`; marked accepted only files while multimodal had a private file/directory resolver | Format 0.3 adds distinct `marked_prepost` and `multimodal_prepost` analysis variants. Both CLIs use one resolver for a result file or directory and finite-validate the versioned `prepost.json` envelope | `prepost_result_roundtrip`; file/directory resolver equality; marked/multimodal CLI version/kind/payload tests; multimodal batch pre/post | N/A | `12d7c4c` | Fixed |
| OUT-03 | Output writing is non-atomic | Yes — writers created the final directory first and exposed result/QC/report files before later optional artifacts, run sidecars, or intermediates could fail | `ArtifactPlan` precomputes validated result/manifest/core requirements. `OutputTransaction` writes to a unique same-parent directory, validates required and declared-written paths, runs marked intermediates/multimodal sidecars/prepost summaries in the same transaction, and renames only after success. Failures clean staging; non-empty/symlink targets are rejected and preserved; empty targets are safely replaced | `failed_artifact_write_does_not_commit_final_directory`; `existing_nonempty_output_directory_is_preserved`; `manifest_matches_written_artifacts`; 14 output, 16 marked CLI, 21 multimodal CLI tests; full Nextest 327/327 | Full transaction benchmark records 0.874/0.859/0.939 ms medians and 12.27 MiB peak RSS with all marked artifacts enabled | `e9e87b0`, `99f6d78` | Fixed |
| OUT-04 | Parquet writer fabricates absent fields | Yes — writer emitted “valid” internal control, false exclusion flags, and zero component/QC IDs for absent Pattern fields | Renamed the only Pattern projection to `write_filtered_pattern_export_parquet`; it marks retained rows explicitly, omits unavailable source/QC state, preserves optional dense absence, and embeds export-kind/semantics provenance. No full-roundtrip API is claimed because `Pattern` cannot recover excluded rows | Enabled `optional_absence_preserved`; supported-field export; invalid metric/length tests; simulation/intermediate CLI; full Nextest 317/317 | N/A | `f4243cd` | Fixed |
| OUT-05 | CSV and Parquet schema definitions can drift | Yes — independent loader state machines and physical schemas changed logical absence | Both physical adapters target the one typed `DecodedCellRow` contract and the same builder; full logical Pattern equality is asserted for equivalent inputs, including optional absence and QC parity | `csv_parquet_equivalent_rows_produce_equal_pattern`, `optional_absence_preserved`, CSV/Parquet QC and dense-option tests | Final 1,024-row CSV/Parquet medians improve 48.9%/51.4%; grouped loader RSS improves 1.3%. The million-row CSV path remains statistically unchanged with +0.5% RSS | `f4243cd` | Fixed |
| OUT-06 | Batch output IDs may escape the output directory | Yes — both batch paths joined unvalidated manifest IDs directly to the output root; `../escaped` wrote outside and exited successfully | One resolver accepts only a trimmed single normal component, rejects slash/backslash, blank, absolute, dot/parent, and existing symlink targets, and canonical-checks existing targets. Both marked and multimodal batches use it; marked jobs prevalidate before parallel work | Enabled `batch_id_cannot_escape_output_root`; unsafe/valid component unit table; symlink test; valid marked parallel and multimodal batch tests | N/A | `a8d38c5` | Fixed |
