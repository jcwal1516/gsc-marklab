# Algorithm Naming Audit

Audit date: 2026-08-22<br>
Evidence SHA: `00cad21`<br>
Remediation reviewed through: completion-audit commits `6d7c13d`, `97dd3e1`,
and `00cad21`<br>
Scope: analytical functions, configuration, result fields, CLI/report language, and validation names.

The audit compares observable implementation with the ordinary technical meaning
of each term. “Mismatch” means the current name must change or the implementation
must be replaced. It does not mean the underlying heuristic is necessarily
useless.

| Public or user-facing name | Implemented method | Accepted technical meaning | Assessment | Required action | Reference or verification |
| --- | --- | --- | --- | --- | --- |
| `modwt`, `wavelet`, `WaveletSummary` | Three heuristic variance shares: nearest-neighbor difference energy, variance of 2x2 block means, and a residual intermediate share; shares are clamped and renormalized | MODWT is a redundant, non-decimated discrete wavelet transform using declared wavelet/scaling filters, levels, and boundary handling; wavelet variance is computed from transform coefficients by scale | Mismatch fixed in `ada6159` | Implemented `multiscale_residual`/literal energy names with no old aliases; documentation explicitly says no wavelet transform occurs | `src/multiscale_residual/energy.rs`; schema/config negative alias and relative-energy tests; [Percival & Walden](https://doi.org/10.1017/CBO9780511841040) |
| `scalogram`, `ScalogramPoint` | A three-point curve containing the same heuristic fine/intermediate/coarse variance shares, with label-permutation envelopes | A scalogram displays wavelet-transform coefficient magnitude or energy as a function of scale and position/time | Mismatch fixed in `ada6159` | Implemented `scale_energy_curve`/`ScaleEnergyPoint`; documentation identifies three heuristic categories, not transform coefficients | `src/api/multiscale_stage.rs::scale_energy_with_envelope`; result and config schema tests |
| `dog`, `territory_radius_from_scale` | Multiplies a configured scale by `sqrt(2)` | Difference of Gaussians subtracts two Gaussian-smoothed fields at declared standard deviations | Mismatch fixed in `ada6159` | Implemented literal `neighborhood_radius_from_scale`; docs state that no Gaussian filtering occurs | `src/multiscale_residual/territories.rs`; conversion test; [Marr & Hildreth, 1980](https://doi.org/10.1098/rspb.1980.0020) |
| `wavelet_territories`, `CandidateTerritory` | At three radii, scans circular neighborhoods around marked cells, calculates a binomial standardized residual, sorts candidates, and greedily suppresses overlaps | A wavelet/DoG territory detector would derive candidates from transform or Gaussian-difference response fields at declared scales | Mismatch fixed in `ada6159` | Implemented residual-neighborhood terminology and distinct `ResidualTerritory`; unavailable QC overlap is null, not zero | `src/multiscale_residual/territories.rs`; typed result/GeoJSON and maxima tests |
| `BartlettPeriodogramSummary`, `marked_bartlett_periodogram` | Rasterizes centered marks, applies one separable Hann taper, computes one 2-D FFT, sorts individual modes by radius, then averages the first `low_k_shells` modes | Bartlett’s estimator averages periodograms from multiple data segments; radial shells aggregate all modes belonging to each radial-frequency band | Mismatch fixed in `35857b4` | Implemented `hann_tapered_raster_periodogram` and actual physical radial-shell means | `src/periodogram/tapered.rs`; independent known-raster shell oracle; [Bartlett, 1948](https://doi.org/10.1038/161686a0) |
| `pair_correlation` / `PairCorrelationPoint` | Mean centered mark product `(m_i-p)(m_j-p)` for pairs in each distance bin | In spatial point-process analysis, the pair-correlation function is the density-normalized second-order function commonly denoted `g(r)` | Mismatch fixed in `2449e3f` | Implemented `mark_pair_covariance`, typed covariance/count fields, and a distinct cross-interaction point DTO; no old aliases | `src/spectra/mark_pair_covariance.rs`; centered-product, empty-bin, schema, config, output, and pre/post tests |
| `centered_structure_factor`, spectrum and shell summaries | Fourier power of centered binary or continuous marks at resolved 2-D modes, normalized by cell count, then aggregated into radial shells and permutation-whitened | A structure factor/spectral density summarizes Fourier-domain spatial fluctuations; radial summaries must retain explicit normalization and shell definition | Matched with explicit implementation scope | Retain neutral spectrum terminology; kernel, shell, chunk, storage, and probabilistic fixtures define the supported calculation | `src/spectra/structure_factor/kernel.rs`; `shells.rs`; `permutation.rs`; binary/continuous reference and mode-to-shell differential tests |
| `anisotropy` | Eigenvalue ratio and principal direction of a weighted low-frequency mode tensor, with label-permutation inference | Anisotropy is direction-dependent structure; no single universal index exists | Accurate descriptive term | Retain, but document the exact tensor, weighting, eligible band, and undefined-direction behavior | `src/spectra/anisotropy.rs`; `src/algorithm_tests.rs` |
| `GlobalEnvelope`, ERL | Extreme-rank-length ordering over observed plus permutation curves with eligibility masking | Global rank-envelope inference controls a functional test across the curve domain; ERL is a defined refinement of extreme rank ordering | Matched | Retain; preserve the checked GET 1.0-7 oracle and document tails/eligibility | `src/permutation/envelopes.rs`; `tests/fixtures/erl_get_1_0_7.json`; [Myllymäki et al., 2017](https://doi.org/10.1111/rssb.12172) |
| `curve_equivalence_test`, `p_equivalence` | Deterministic classification of whether one max standardized-difference statistic is below a supplied margin; no equivalence null distribution and no p-value | A statistical equivalence test rejects a null of non-equivalence under a declared design, error rate, and margin | Mismatch fixed in `2191974` | Implemented `curve_margin_assessment`; format 0.3 removes `p_equivalence` and uses descriptive `margin`/`within_margin` fields | `src/comparison/margin_assessment.rs`; red/green schema and boundary tests; [Schuirmann, 1987](https://doi.org/10.1002/jps.2600760216) |
| `curve_difference_test` | Approximate permutation diagnostic obtained by pooling and shuffling already-aggregated curve bins | A formal difference test requires a justified exchangeability unit and null model | Mismatch fixed in `69005bb` | Implemented `pooled_bin_difference_diagnostic`, typed comparison methods, and `pooled_bin_p_value`; limitation text remains mandatory | `src/comparison/pooled_bin_difference.rs`; schema, deterministic, boundary, pre/post, report, and CLI tests |
| `beta_binomial`, `BetaBinomialSummary` | Independent Beta(1,1)-Binomial conjugate posterior summaries for the whole pattern and fixed groups; no fitted beta-binomial count model or learned overdispersion | A beta-binomial model uses a beta mixing distribution for binomial probability, producing beta-binomial marginal counts and usually an estimable dispersion relationship | Mismatch fixed in `1e8fbbd` | Implemented `beta_posterior_group_summary`; config/result/report surfaces use beta-posterior-group terms and retain explicit exploratory scope | `src/diagnostics/beta_posterior.rs`; deterministic posterior, grouping, schema, config, CLI, and report tests |
| `graph_smoothing`, `message_passing_score` | Two deterministic rounds of 0.5 self plus 0.5 unweighted neighbor-mean propagation on compact one-hot label IDs; edge score is a normalized product summary | Graph smoothing/message passing broadly means local propagation or averaging over graph adjacency | Accurate with deterministic qualifier | Retain `deterministic_graph_smoothing_v1`; reports state that it is not a trained graph neural network | `src/diagnostics/graph_smoothing.rs`; deterministic score and compact-label application tests |
| `rigid` registration | Orientation-preserving 2-D least-squares rotation plus translation; determinant +1; no scale or reflection | A 2-D rigid transform preserves Euclidean distances and orientation when reflections are excluded | Matched after COR-02 | Retain and preserve known-transform, noise, scale-rejection, degeneracy, and reflection tests | `src/registration/transform.rs::fit_rigid`; `src/registration/tests.rs` |
| `affine` registration | Six-parameter 2-D affine least-squares fit | Affine registration permits translation, rotation, scale, and shear while preserving affine combinations | Matched | Retain; add/retain known-transform and degeneracy fixtures | `src/registration/transform.rs::fit_affine`; registration tests |
| `validation`, `validate-synthetic`, multimodal validation results | Original marked scenarios included tautological/manual pass rules and multimodal scenarios synthesized booleans without calling production | Validation evaluates production behavior against independent expected properties with reported denominators, failures, and justified acceptance criteria | Bypass and manual synthesis fixed in Phase 9; quick suite remains accurately named smoke | Marked/multimodal generators construct inputs, invoke production engines/comparisons, and derive criteria from results. Reports expose failures and Wilson intervals. A 1,000-replicate random-label control is scheduled calibration; smoke results make no calibration claim | `src/synthetic_smoke/{marked,multimodal,multimodal_observation}.rs`; `generators/`; production-call, scenario, failure-denominator, and scheduled calibration tests; `docs/validation-methodology.md` |
| generic marked `Interpretation` | Core engine emitted threshold-driven text referring to “configured MMR-IHC phenotype” and clonality even though `Pattern` and `mark_label` are generic | A generic marked-pattern engine should emit neutral spatial classes; MMR-specific prose belongs in an explicit presentation/policy layer | Mismatch fixed in `ada6159` | Core result classes and marked report now use neutral spatial language; MMR prose remains only on explicitly multimodal/MMR surfaces | `src/api/interpretation.rs::interpretation_for`; generic result/report and multimodal report tests |
| `detect_mmr_abnormal_territories` | DBSCAN-like density-connected clustering of explicitly MMR-abnormal IHC cells using `eps_um` and `min_cells`, followed by centroid/radius construction | “Territory” is a domain label rather than a standardized estimator; DBSCAN has explicit density-reachability semantics | Accurate within the explicitly MMR-specific contract | Keep the qualified policy name and its dedicated `NeighborhoodTerritory` result with explicit abnormal-cell support and cluster identity | `src/neighborhood/territories.rs`; result/CLI regressions; MODEL-01 fixed in `51564c8` |
| `NeighborhoodTerritory` and `ResidualTerritory` | Separate multimodal density-cluster and marked residual-neighborhood results with algorithm-specific fields | Distinct algorithms require fields with algorithm-specific meaning and no fabricated future fields | Match | Retain separate DTOs; removed generic `TerritoryFeature`, `z_or_power`, derived `scale_um`, and unimplemented QC overlap without aliases | `src/output/result_types/{marked,multimodal}.rs`; exact distinct-type, GeoJSON, CLI, profile, and pre/post tests |
| `spatial_index` | Exact immutable 2-D `rstar` index with deterministic distance/index ordering wrapped around nearest, k-nearest, indexed-radius, and external-point-radius queries | A spatial index organizes coordinates to answer nearest, k-nearest, and radius queries without scanning every point for every query | Matched after PERF-01 | Retain one shared backend and its brute-force differential/tie contracts | `src/geom/spatial_index.rs`; nearest/radius/kNN/duplicate/order differentials and scaling benchmarks |
| neighborhood enrichment | Counts configured label-pair edges on a declared graph and compares with label permutations, returning expected count, optional ratio/z-score, and permutation p-value | Edge enrichment is a descriptive observed-versus-null edge-count comparison | Matched after COR-04 | Retain; document graph construction, label-pair symmetry, primary null, and undefined ratio/z-score states | `src/neighborhood/enrichment.rs`; result-format 0.3 docs |
| cross-interaction curves and global envelope | Counts cross-label pairs in physical radius bins over one indexed pair plan; source-section-stratified label permutations feed the checked ERL global envelope with explicit geometry eligibility | A cross-interaction curve is a broad descriptive name; “global envelope” requires one functional rank-envelope calculation over the curve domain | Matched after SCI-10/PERF-11 | Retain neutral cross-interaction terminology. Empty geometric bins are unavailable; ERL bounds and `p_global` have one owner and are not called a classical normalized `g_ij(r)` | `src/neighborhood/cross_curves.rs`; brute-force, ERL oracle reuse, empty-bin, serialization, one-plan, budget, and scaling tests |

## Phase 3 decisions implied by the audit

1. Follow the plan’s default remediation path: preserve the existing heuristic
   calculations but rename them to multiscale residual/scale-energy terms.
2. Rename the DoG radius helper; do not implement Gaussian convolution solely to
   preserve a decorative name.
3. Rename the raster spectral diagnostic and correct its radial shell aggregation.
4. Make the marked engine’s interpretation neutral; keep MMR language only in an
   explicitly MMR-facing report policy.
5. Treat the additional pair-correlation, beta-binomial, equivalence, difference,
   and validation mismatches as audit-discovered findings. Add them to the findings
   matrix rather than silently folding them into unrelated IDs.

## Reference policy

Established names are retained only when implementation tests establish the
defining operation. External references above define terminology; repository
fixtures and differential tests establish that Marklab actually performs it.
